# Demand Atlas — Review Engine

継続改善レビューの**手順とルール**を定義する固定ドキュメント。

- このファイルは**変わらない**（運用ルールの改定時のみ更新する）
- 毎回変わる値（測定値・ベースライン・巡回位置・保留中の項目）は `REVIEW_STATE.md` に置く
- レビュー時は本ファイルの手順に従い、`REVIEW_STATE.md` を読み込んで前回との差分を判断し、最後に `REVIEW_STATE.md` を更新する

---

## 0. 目的

**「次に何を改善すればプロジェクト価値が最大化するか」を見つけること。**

以下は目的ではない。

- レビューを短くすること
- レビュー項目を維持すること
- 改善案を無理に出すこと
- 異常を見つけること

異常がなくても、改善余地・将来リスク・レビュー方法の改善を必ず検討する。
本当に改善余地がないと判断する場合は、「改善なし」ではなく**改善しない理由を実測付きで**述べる。

---

## 1. 絶対ルール

| # | ルール |
|---|---|
| R1 | **推測禁止**。コード・実データ・実行結果・ブラウザ実測のみを根拠にする |
| R2 | 実測できないものは「**実測不能**」と明記し、**代替の測定手段があるかを必ず検討する** |
| R3 | 検証していない事柄は「**仮説**」と明記して事実と分離する |
| R4 | **実装は `実装GO` が出たものだけ**。レビュー中はコードを変更しない |
| R5 | **YAGNI**。将来のためだけの抽象化・レイヤー追加・過剰設計は却下する |
| R6 | HTTP ステータスや「成功」表示だけで正常と判断しない。**必ず中身を確認する**<br>（過去に robots.txt を 200 だけで存在すると誤報告した前例あり） |
| R7 | **生産側だけでなく消費側を見る**。CI の成否ではなくユーザーが受け取った結果を見る<br>（過去に「CI 緑＝ユーザーに届いた」と 8 回検証せず仮定した前例あり） |
| R8 | 測定済みで変化していない項目は**再測定しない**。浮いたコストを未測定領域に回す |
| R9 | 改善案には必ず**客観的な数値条件**（いつ実装するか）を付ける |
| R10 | 優先順位は**技術的な正しさではなく現在のプロジェクトフェーズとの整合**で決める |
| R11 | **次回レビュー用プロンプトを生成しない**。運用は本ファイルと `REVIEW_STATE.md` の 2 つだけで行い、レビュー結果は STATE の更新として残す<br>（過去にプロンプトを毎回作り直し、短縮そのものが目的化した前例あり） |

---

## 2. モード

レビュー開始時に**現在のモードを必ず表示する**。

| モード | 動作 |
|---|---|
| **Manual Review** | ユーザーの指示時のみレビューする |
| **Autonomous Review** | レビュー後 `REVIEW_STATE.md` を更新し、次回へ引き継ぐ（**既定**） |
| **Development Mode** | レビューに加えて実装候補の設計まで行う（実装はしない） |
| **Implementation Mode** | `実装GO` が出た改善のみ実装する。完了後 Autonomous Review へ戻る |
| **Brainstorm Mode** | 改善案を大量に出す。実測と優先順位付けは行わない |
| **Access Analytics** | 「**アクセス分析**」で起動。「今日訪れた人」を**実際に数えられている状態**まで仕上げる（下記 第 3.7 章）。**レビューだけで終わらない。自分で出来ることは全て自動実行し、アカウント操作だけが残った時点で停止する** |
| **Work Session** | 「**/work 時間 作業名**」で起動。指定時間の間、指定された作業だけを自律的に繰り返す（下記 第 3.6 章）。**途中で確認を求めて停止しない。停止条件は時間だけ** |
| **Demand Expansion** | 「**需要拡張**」の 1 語で起動。追跡テーマの拡張だけを目的に、分析から検証・STATE 更新までを一連で実行する（下記 第 3.5 章）。**このモードだけは `実装GO` を待たずに実装まで進む**が、対象は同章 STEP 7 の条件を満たすものに限る |

モード変更が指示されたら、エンジン全体をそのモードへ切り替える。

---

## 3. レビュー手順

### PART A — コア監視（毎回）

`REVIEW_STATE.md` の「コア監視コマンド」を実行し、同ファイルの前回値と比較する。

監視の対象は 2 系統。**両方を毎回見る**。

1. **生産側** — CI の成否と実行時間、未コミットの有無
2. **消費側** — 本番が配信しているデータ、および**本番ページの実描画**

消費側の描画確認は JSON だけでは代替できない（描画回帰を検出できないため）。

該当する異常がなければ**深掘りせず PART B へ進む**（R8）。

### PART B — 領域ローテーション（毎回 1 領域）

`REVIEW_STATE.md` の巡回表から**今回の担当領域を 1 つだけ**選び、深掘りする。

対象領域（一巡したら先頭へ戻り、前回と異なる切り口で見る）:

```
CI / コード品質 / データ品質 / UX / SEO / アクセシビリティ / セキュリティ /
API / 保守性 / スケーラビリティ / コスト / 運用・監視 / ドキュメント /
開発フロー / リリース方法 / レビュー方法
```

**巡回表の外で気づいたことがあれば、それを優先してよい。**
チェックリストの内側だけを見ると盲点が固定化する。

### PART C — 将来リスクの棚卸し（毎回）

`REVIEW_STATE.md` の以下 3 区分を確認・更新する。

| 区分 | 内容 |
|---|---|
| **発火予定** | 日付が確定しているもの（時間経過だけで発生する事象を含む） |
| **数値条件** | しきい値到達で着手するもの。現在値と条件を併記する |
| **仮説** | 未検証。実測できたら事実へ格上げする |

**規模軸（テーマ数・ユーザー数）だけでなく時間軸も見る。**
テーマを増やさなくても時間経過だけで発生する問題がある。

### PART D — 逆算チェック（毎回）

**「計画中の機能に必要なデータを、今日取得しているか」**を問う。

異常検知は現状維持の確認にすぎない。逆算チェックだけが「**今日やらないと将来できなくなること**」を発見できる。取得していないデータは、後から遡及できない場合がある。

### PART E — 改善案と優先順位

#### E-1. 改善案の分類

| 分類 | 定義 |
|---|---|
| **即実装** | 今すぐ着手すべき。**最大 1 件** |
| **条件付き実装** | 数値条件を満たしたら着手（条件必須） |
| **将来検討** | 記録のみ。条件はまだ定義できない |
| **却下** | 実施しない。**理由必須** |

#### E-2. 評価軸

各案を以下で評価する。

```
ユーザー価値 / 収益への影響 / 開発速度 / 運用コスト / 保守性 /
スケーラビリティ / SEO / 実装コスト / ROI
```

#### E-3. 優先順位の決定（R10）

**技術的な緊急性より、現在のフェーズとの整合を優先する。**

必ず以下を順に問う。

1. 現在のフェーズは何か（MVP 検証 / ユーザー獲得 / 収益化 / スケール）
2. そのフェーズでの最優先目標は何か
3. 改善しない場合の損失は何か
4. 改善した場合に得られる価値は何か
5. **その改善は今の目標達成に本当に必要か**

「不可逆だから今やる」は理由として不十分。
**失われるものを使う機能に、現時点で価値があるか**を検証すること。

#### E-4. ボトルネック

改善案の有無にかかわらず、**現在の最大のボトルネックを 1 つだけ**答える。
前回から変化していない場合は、**変化しなかった理由**を説明する。

### PART F — 自己評価

100 点満点で採点し、以下を必ず答える。

- 今回見つけられた問題
- 見逃した可能性がある問題
- 今回初めて見た領域は何か（広げなかった場合はその理由）
- **チェックリストの外側で見つけたものはあるか**
- **「動いているから正常」と仮定して検証を省いた箇所はあるか**（R6・R7）
- **「実測不能」とした項目に代替手段はなかったか**（R2）
- 新しく追加すべきレビュー項目
- 不要になったレビュー項目
- 次回改善したいレビュー方法

前回より点数が下がった場合はその理由を述べる。

#### 停滞検知

**3 回連続で新規の発見がなかった場合**、レビュー方法が停滞している。
巡回表を小修正するのではなく、**巡回表を破棄し、一度も見ていない領域を列挙して再設計する**。

---

## 3.5 Demand Expansion モード（トリガー: 「需要拡張」）

### 最終目標

**Demand Atlas が追跡できる需要・情報源・分析量・情報量を、継続的に増やし続けること。**
1 回の実行で終わる作業ではなく、実行を重ねるほど扱える情報が増える状態を保つ。

テーマ追加専用のコマンドではない。**次の 7 つすべてを「情報量」として扱う。**

| 種類 | 例 |
|---|---|
| 新しい需要（テーマ） | 追跡テーマの追加 |
| **新しい情報源** | 公開データ / RSS / API / データベース / 論文 / オープンデータ |
| **新しいカテゴリ** | 空白カテゴリを埋める |
| **新しい分析方法** | 判定・勢い・参入しやすさ等の新しい指標 |
| **新しい比較方法** | 比較軸・比較の切り口 |
| **新しい可視化** | グラフ・図・表の新しい種類 |
| **新しいデータ** | 既に取得しているのに表示していない値を出すことを含む |

加えて **需要発見アルゴリズムそのものの改善**も毎回対象にする（STEP 1・STEP 4）。

### 唯一の評価基準 — 前回より情報量が増えたか

**毎回この表を実測し、STATE の「情報量ベースライン」の前回値と数値で比較する。**
テーマ数を増やすこと自体は成果ではない。**テーマ数だけを増やす変更は禁止。**

| 指標 | 測り方 |
|---|---|
| 公開テーマ数 | `demands.json` の `demands.length` |
| **観測できている情報源数** | `_*Detail` のキー種類（派生の `_growthDetail` を除く）+ ニュース RSS |
| 使用カテゴリ数 | `category` のユニーク数（分母は 9） |
| 平均ソース数 | `_*Detail` の平均個数 |
| **分析の種類数** | `_insights` の分析キー数（`version` / `generatedAt` / `method` はメタなので除く） |
| **比較軸の数** | `Compare.jsx` の `ScoreCell` 行数 + 比較セクション数 |
| **可視化の種類数** | `src/` に実在するグラフ・図のクラス種類（STATE に一覧を持つ） |
| アイデア件数 | `_insights` の monetization + content + saas の合計 |
| 日次系列を持つテーマ数 | `_wikipediaDetail.byDate` が 2 点以上 |
| UI が参照するフィールド数 | 全キー走査 → `src/` 全文との照合 |
| Home 総転送 | ブラウザ実測（cold） |

**1 つも増えていない場合、その変更は成果ゼロとして扱い、コミットしない。**
**増えた場合は、どの指標が何から何に増えたかを数値で報告する。**

### 絶対ルール（第 1 章 R1〜R11 に追加）

| # | ルール |
|---|---|
| X1 | **推測禁止・実測のみ**。採否は必ず数値で示す |
| X2 | **既存設計を壊さない**。テーマ定義・envelope・history スキーマの互換性を破らない |
| X3 | **score 計算は変更しない**（`build-demands.mjs` の式・重み・飽和点） |
| X4 | **既存パイプラインを最大限再利用する**。ただし**既存情報源では限界だと実測で判断した場合は、新しい公開データ・RSS・API・データベース・論文・オープンデータを自律的に調査し、採用可否を評価する**（下記 STEP 2.5）。絶対条件（完全無料 / API キー原則不要 / スクレイピング禁止 / ToS 遵守）は例外なく維持する |
| X5 | **追加価値が無ければ「今回は追加しない」と結論づける** |
| X6 | **1 回の実行でテーマを増やすのは最大 1 件**（原因の切り分けのため） |
| X7 | **内部データだけ更新して終了することを禁止**。STEP 6 の UI 反映まで到達して初めて完了 |
| X8 | 測定できない評価軸は**「判断材料不足」と書く**。代用の数値を勝手に作らない |

---

### STEP 1 — コマンド自身のレビューと改善（ループ）

次の 7 観点を**実測で**洗う。

| 観点 | 測り方 |
|---|---|
| 改善できる点 | `themes:eval` を実行し、判断に必要な数値が出力に無いものを数える |
| 不要な処理 | 出力のうち、判断に一度も使わなかった項目を数える |
| 重複 | 同じ数値を 2 箇所以上で計算していないか |
| 精度不足 | 候補上位に事件名・汎用語・既存テーマ重複が残る件数 |
| 自動化できる処理 | この実行中に手で書いた ad-hoc スクリプトの数 |
| 手作業 | 人間の判断を要した箇所の数 |
| ボトルネック | 最も時間のかかった STEP |

**改善可能なら改善し、再レビューする。** 終了条件は次のいずれか。

- 1 周で検出した改善点が **0 件**
- **2 周連続**で新規の改善点が 0 件
- **3 周に到達**（それ以上は 1 サイクルに詰め込まない。残りは STATE に繰り越す）

### STEP 2 — 新しい需要候補の探索

- 既存カテゴリだけでなく**空白カテゴリ**も対象にする（`themes:eval` が空白を出力する）
- 情報源は既存を最大限利用する。未紐付け記事・`keyword-trends` が一次資料
- 追加情報源の調査は、既存資料で候補が枯れた時のみ行う（X4）

### STEP 2.5 — 情報源の自律調査（限界を実測したときだけ）

**発動条件（どちらかを実測で満たした時）**

1. STEP 2 の候補が **0 件**（却下語を除いた上位にテーマとして成立する語が無い）
2. 空白カテゴリが、現在の情報源では原理的に埋まらないと結論済み

**調査手順**

```
1  STATE「情報源の調査結果（再調査不要）」を先に読む — 済みのものは再調査しない
2  候補を挙げる: 公開データ / RSS / API / データベース / 論文 / オープンデータ
3  絶対条件で機械的に落とす（有料・キー必須・スクレイピング前提・ToS 不許可）
4  生存確認: 実際に 1 回叩き、200 と中身を確認する（HTTP 200 だけで判断しない）
5  日本語・日本市場に信号があるか（英語圏専用は不採用）
6  既存情報源との重複を測る（同じテーマで同じ順位を返すだけなら価値ゼロ）
7  RSS の場合は「テーマ紐付き率」を実測する。**0% は不採用**
8  採否と理由を STATE の調査結果表へ記録する（却下も残す）
```

**採用したら STEP 5 以降と同じ扱い**にする。envelope v1.0.0 に合わせ、
`metricKind` を明示し（stock は静止率の計算から除く）、`check-sources` に載せ、
history へ記録し、UI に出るところまで到達して初めて完了（X7）。

### STEP 3 — 候補の評価（8 軸）

**各軸に測定手段と測定できる時期を定める。時期が来ていない軸は「判断材料不足」と書く（X8）。**

| 軸 | 測り方 | 測れる時期 |
|---|---|---|
| **継続性** | 分散 = 出現日数 ÷ 出現件数。実測で事件 0.1〜0.29 / 継続語 1.0。しきい値 0.6 | 今すぐ |
| **情報量** | 未紐付け記事での出現件数（`seedFilter.minOccurrences` 以上） | 今すぐ |
| **カテゴリ適合性** | 空白カテゴリを埋めるか（0/1） | 今すぐ |
| **重複** | 既存テーマの keywords / relatedKeywords / 却下語との一致数 | 今すぐ |
| **取得コスト** | 追加編集が必要なファイル数（Phase A は 6・Phase B は 2）と API 呼び出し数 | 今すぐ |
| **将来性** | `keyword-trends` の velocity（件/日）。**既知語のみ測定可。新語は判断材料不足** | 語が trends に入った後 |
| **取得可能性** | 各情報源の検索語で 0 件でないか | **Phase A** |
| **需要性** | Qiita 記事数 + App Store アプリ数（作る人・売る人がいるか）。**ニュース記事の意図語共起では測れない**（2026-07-30 実測: ゲーム 7% / アニメ 0% / 音楽 0% に対し無関係な新聞 16% / SNS 29% と誤検出） | **Phase A** |

**採用・却下の理由を必ず記録する。** 却下語は `config/theme-registry.json` の
`seedFilter.rejectedWords` に書く（散文に書くと次回の重複除去が機能しない）。

### STEP 4 — 需要発見アルゴリズムの改善（ループ）

今回の探索から、次が改善できるならコマンド自身へ反映する。

```
検索方法 / 評価方法 / ランキング / 重複除去 / 候補生成 / カテゴリ分類
```

反映先は `scripts/evaluate-themes.mjs` と `config/theme-registry.json` の
`seedFilter`。**しきい値をハードコードせず設定に置く**（次回の調整を実装なしで行うため）。
終了条件は STEP 1 と同じ（0 件 / 2 周連続 0 件 / 3 周上限）。

### STEP 5 — テーマ追加（2 段階・X6）

**「昇格基準を実測で満たす」を 1 段階目の条件にはできない。**
マッピングに入れるまでソース数は測れない。既存アーキテクチャが
**取得＝マッピング駆動 / 公開＝辞書駆動**で分かれているため、次の分割が成立する。

#### Phase A: 観測開始（公開しない）

編集するのは情報源マッピングだけ（`config/*-mapping.json` × 5 と
`fetch-wikipedia-pageviews.mjs`）。`extract-demand-candidates.mjs` の辞書には**入れない**。

条件:
1. `seedFilter` の基準（件数・分散）を満たす
2. 空白カテゴリを埋める、または既存テーマと重複しない
3. score 計算・envelope・history スキーマに変更が無い
4. 各情報源の検索語を実際に叩いて 0 件でないことを確認済み

実行後 `status: observing` で登録し、次回以降の `themes:eval` でソース数と需要性を**実測**する。

#### Phase B: 公開（1 回 1 件）

条件:
1. **実測で** `promotionCriteria.minSources` 以上（Phase A の観測結果）
2. hot キーワードが実際の記事にヒットする（0 件では公開しても即 `stalled`）
3. 波及箇所を事前 grep で列挙済み
4. 検証手段が事前に決まっている

**1 つでも欠けたら実装しない。** `candidate` のまま次回へ送る。

### STEP 6 — Demand Atlas への反映（X7）

テーマ・カテゴリ・情報量を変えた場合、**ユーザーが見る面すべて**を確認する。
「内部データだけ更新して終了」を禁止する。

| 面 | 確認方法 |
|---|---|
| Home | カード枚数 = themes 数 / カテゴリチップに新カテゴリが出るか |
| ランキング | 新テーマが出るか。詳細ページが無いテーマが `theme-link-plain` になるか |
| 詳細ページ | 系列グラフ・情報源別観測・アイデア・スコア内訳が描画されるか |
| 比較 | セレクタに新テーマが出るか |
| タイムライン / 履歴 | 新テーマの jsonl が生成され日別に出るか |
| カテゴリ | 空白カテゴリが埋まれば `cat-card` が増え「観測準備中」から消えるか |
| 検索（Explore） | キーワード検索と状態フィルタでヒットするか |
| アイデア | `/ideas` の件数が 8 件 × 追加テーマ数だけ増えるか |
| グラフ・統計・表示文 | Hero の情報源数・「N テーマ」表記・footer の情報源列挙が実態と一致するか |
| データ生成 / history / pipeline | `npm run all` が通り、`check-sources` が全ソース健全を返すか |

### STEP 7 — 整合性確認

`UI` / `データ` / `説明文` / `meta` / `README` / `REVIEW_STATE` / `コマンド一覧`
が一致していることを確認する。**片方だけ更新した状態を残さない。**

### STEP 8 — 動作検証

```bash
npm run check && npm run build && npm run themes:eval
```

加えてブラウザで `console error` / 表示崩れ / リンク切れ / score・status の不変を確認する。
本番反映後は本番でも同じ確認を行う（R7: 消費側を見る）。

### STEP 9 — STATE 更新

改善内容 / 新ルール / 却下理由 / 実測結果 / 反省 / 次回改善点を `REVIEW_STATE.md` へ。
**情報量ベースラインの表を必ず更新する**（成果ゼロならゼロと書く）。

### STEP 10 — コマンド一覧の更新

このコマンドの動作を変えた場合、STATE の「コマンド一覧」の説明も更新する。
**一覧と実際の動作が食い違う状態を禁止する。**

### 1 サイクルの終了条件

改善点が十分小さくなり（STEP 1・4 の終了条件）、かつ
レビュー → 改善 → 実装 → 検証 → UI 反映 → STATE 更新 → コマンド一覧更新
まで到達した時点で 1 サイクル終了。

**「ユーザーが利用できる状態」が完了の定義。** 追加したものは必ず次の 6 つに反映する。
1 つでも欠けていたら未完了として扱う。

```
サイト（本番で見えるか） / データ（pipeline が生成するか） / UI（該当ページに出るか）
README（説明が実態と一致するか） / REVIEW_STATE（ベースラインと状態） / コマンド一覧
```

### 完了レポート（毎回・簡潔に）

改善した内容 / 追加した需要 / 追加したカテゴリ / 追加した情報量 /
アルゴリズム改善 / UI へ反映した内容 / 検証結果 / 次回改善候補

---

## 3.6 Work Session モード（トリガー: 「/work 時間 作業名」）

### 目的

**指定された時間の間、指定された作業だけを自律的に繰り返す。**

```
レビュー → 改善 → 実装 → 検証 → レビュー → 改善 → …
```

### 形式

```
/work 30分 レビュー
/work 2時間 需要拡張
/work 6時間 実装
/work 90分 UI改善
```

作業名が既存モードと一致する場合はその手順を使う。
一致しない場合（UI改善 / 実装 など）は自由作業として扱い、本章のループだけを適用する。

| 作業名 | 使う手順 |
|---|---|
| レビュー | 第 3 章 PART A〜G |
| 需要拡張 | 第 3.5 章 STEP 1〜10 |
| 上記以外 | 本章のループのみ（対象は作業名が示す範囲に限る） |

### 絶対ルール（第 1 章 R1〜R11 に追加）

| # | ルール |
|---|---|
| W1 | **停止条件は時間だけ。** 改善が終わっても次の改善点を探して続ける |
| W2 | **途中で「終わりました」「他にありますか？」と確認して止めない。** 判断は自分で行う |
| W3 | **作業目的を途中で変えない。** 関係ない改善点を見つけたら `REVIEW_STATE.md` に記録して元の作業へ戻る |
| W4 | **変更したら必ず検証する**（下記の品質確認）。未検証のまま次の改善へ進まない |
| W5 | **内部だけ更新して終わらない**（下記の反映） |
| W6 | **区切りごとにコミットする。** 1 サイクル = 1 コミットを目安に、作業の途中状態を残さない |
| W7 | **経過時間を実測で管理する。** 開始時刻を STATE に記録し、毎サイクルで `date -u` と突き合わせる |

### 品質確認（変更のたびに全項目）

```bash
npm run check && npm run build && npm run themes:eval
```

加えてブラウザで確認する。

| 対象 | 見るもの |
|---|---|
| build | 成功するか・バンドルサイズの変化 |
| pipeline | `check-sources` が全情報源健全を返すか |
| history | jsonl の件数とテーマ数が減っていないか |
| score / status | 表示層の変更なら**不変であること** |
| console error | 0 であること |
| 表示崩れ | 375px と 1280px で `scrollWidth <= innerWidth`・はみ出し要素 0 |
| リンク切れ | `/demand/` へのリンクが実在するテーマを指しているか |

### 反映（変更が影響する場所すべて）

Demand Atlas / Home / ランキング / 比較 / 詳細ページ / 検索 / タイムライン / 履歴 /
アイデア / カテゴリ / README / REVIEW_STATE / コマンド一覧。

**該当しない面は「該当なし」と根拠付きで書く**（例: `src/` を触っていないなら UI 変更なし）。

### 自分自身のレビュー（定期）

サイクルの合間に実測で問う。

- もっと良い方法はないか
- 不要な処理はないか
- 自動化できることはないか
- 品質を上げられないか

改善できるならその場で改善する（ただし W3 の範囲内に限る）。

### 時間を跨ぐ継続（実際の制約に合わせた設計）

**1 回の応答で数時間走り続けることはできない。** 応答はツール呼び出しを止めた時点で終わる。
そのため長時間の `/work` は次の形で継続する。

1. 開始時に `REVIEW_STATE.md` の「/work の作業状態」へ**開始時刻と終了予定時刻**を記録する
2. 各サイクルの終わりに**進捗を上書き保存**する（下記 6 項目）
3. 応答が切れたら、ユーザーは同じ `/work` を再送する。**残り時間は STATE の終了予定時刻から実測で計算**するので、指定時間の総量は守られる
4. 定期実行が使える環境なら `/loop` と組み合わせて再送を自動化する

**`/compact` は環境側のコマンドで、こちらからは実行できない。**
代わりに、コンテキストが増えても失われないよう**毎サイクルで状態を STATE に保存**する。
文脈が失われた場合（環境側の要約・リセット）は、STATE の「/work の作業状態」を読んで中断点から再開する。

### 保存する状態（6 項目・毎サイクル上書き）

```
目的       この /work で何をするか（作業名そのまま）
進捗       開始時刻 / 終了予定 / 経過 / 何サイクル目か
完了済み   コミット済みの改善（コミットハッシュ付き）
未完了     着手したが終わっていないもの
次に行うこと 再開時に最初にやること
重要な注意点 落とし穴・検証で判明した制約
```

### 完了レポート（時間終了時・簡潔に）

実施時間 / レビュー回数 / 改善回数 / 実装数 / 変更ファイル数 / コミット数 /
追加機能 / 修正内容 / 検証結果 / 次回開始地点

---

## 3.7 Access Analytics モード（トリガー: 「アクセス分析」）

### 目的

**「今日訪れた人」を、実際に数えられている状態まで仕上げる。**
レビューで終わらせず、動く状態になるまで作業を続ける。

### STEP 1 — 現状を実測する

`API` / `UI` / `保存先` / `Vercel` / `Redis` / `環境変数` / `本番` / `ビルド` / `console error`
を**推測せず実測**する。実測の入口は次の 2 つ。

```bash
npm run visits:check                  # 本番が「今この瞬間に数えているか」
curl -s https://demand-atlas.vercel.app/api/visit?diag=1   # 変数名と接続可否（値は返らない）
```

### STEP 2 — 不足を一覧化する

「何が無いから数えられていないか」を 1 行ずつ挙げる。

### STEP 3 — 自分で出来ることは全て自動実行する

レビュー / 修正 / コード変更 / ビルド / 検証 / README 更新 / STATE 更新 /
コマンド一覧更新 / 本番確認。**ここで止まらない。**

### STEP 4 — アカウント操作だけが残ったら停止する

Vercel・Redis・ログイン・クリックなど**こちらで実行できない操作だけ**になったら、
次の形式**だけ**を表示する。余計な説明は書かない。

```
【あなたが行う操作】
① …
② …
所要時間：約 N 分
```

### STEP 5 — 「完了」を受けたら自動で続きを実行する

追加のレビュー → 修正 → 検証 → 本番確認まで、指示を待たずに進める。

### STEP 6 — 問題が見つかったら繰り返す

レビュー → 原因特定 → 修正 → 検証 を、**重大な問題が 0 件になるまで**繰り返す。

### STEP 7 — 完了時の一覧

```
【実装結果】
追加した機能 / 修正した内容 / 現在利用できる分析項目 /
今後追加できる分析項目 / 本番状態 / 未完了事項
```

### 絶対ルール

| # | ルール |
|---|---|
| A1 | **推測禁止・必ず実測**。「動くはず」で完了にしない |
| A2 | **UI に出るまでが完了**。API が動くだけでは未完了 |
| A3 | **本番で確認するまでが完了** |
| A4 | 数えられていない時に**推定値やゼロを表示しない** |
| A5 | 個人を特定できる情報（IP・UA・Cookie・識別子）を保存も送信もしない |
| A6 | ユーザーが操作するのは**アカウント操作だけ**。それ以外は全て自動実行する |

---

### PART G — STATE 更新

`REVIEW_STATE.md` を第 5 章のスキーマに従って更新する。
更新後、変更点の要約を提示する。

---

## 4. 履歴比較

毎回、前回レビューとの差分を以下に区別して報告する。

| 区分 | 内容 |
|---|---|
| 改善された点 | |
| 悪化した点 | |
| 新しく見つかった問題 | |
| 新しく確認できた良好点 | |
| 変化しなかったもの | |

---

## 5. REVIEW_STATE.md のスキーマ

STATE は以下の構造を保つ。**エンジン側に測定値を書き戻さない。**

```
## メタ
  最終レビュー日 / モード / フェーズ / 最優先目標 / 最大ボトルネック（と不変期間）

## コア監視コマンド
  PART A で実行するコマンド（生産側・消費側）と、その異常判定しきい値

## 前回測定値
  CI / 本番 / データ品質 / パフォーマンス の実測値
  ※ 比較の基準。次回はここと突き合わせる

## 領域ローテーション
  領域ごとの「実施日 / 主な発見 / 次に見る切り口」と、次回の担当領域

## 将来リスク
  発火予定（日付確定） / 数値条件（現在値併記） / 仮説（未検証）

## 逆算チェック
  計画中の機能と、それに必要なデータの取得状況

## 保留中の改善案
  即実装（GO待ち） / 条件付き（条件併記） / 将来検討 / 却下（理由併記）

## 決定済みの方針
  過去のレビューで確定した運用ルール（実測根拠を併記）

## 測定状況
  測定済み（再測定不要） / 未測定 / 実測不能（理由と代替案）

## 自己評価履歴
  回ごとの点数と、次回改善する点
```

---

## 6. 拡張規約

ユーザーから追加要望が来た場合:

- **既存のエンジンを壊さない**
- 既存ルール（第 1 章 R1〜R10）は維持する
- 新しい観点は PART B の巡回表へ、新しい判定は PART A のしきい値へ、
  新しい分類軸は PART E-2 の評価軸へ**追加**する形で組み込む
- 手順そのものを変える要望の場合のみ、本ファイルを改定する

---

## 7. 運用の起動手順

```
1. REVIEW_ENGINE.md（本ファイル）を読む
2. REVIEW_STATE.md を読む
3. モードを表示する
4. トリガーに応じて分岐する
     「レビュー」   → PART A → B → C → D → E → F → G
     「需要拡張」   → 第 3.5 章 STEP 1 → 10
     「/work …」    → 第 3.6 章 Work Session
     「アクセス分析」 → 第 3.7 章 Access Analytics
     「実装GO」     → Implementation Mode
5. REVIEW_STATE.md を更新する（← 成果物はこれだけ）
```

**コマンドの正本は `REVIEW_STATE.md` の「コマンド一覧（正式版・唯一の情報源）」に置く。**
本ファイルは手順の分岐だけを持ち、コマンドの追加・削除では改定しない（二重管理を避けるため）。

コマンドは**その単語だけが送信された時に実行する**。通常の会話の中に出てきても実行しない。
いずれも**追加の指示を必要としない**。何を実行するかは本ファイルと `REVIEW_STATE.md` が決める。

**次回レビュー用のプロンプトは生成しない**（R11）。
次回に引き継ぐ情報はすべて `REVIEW_STATE.md` に書く。
引き継ぎたい内容が STATE のどのセクションにも収まらない場合は、
STATE のスキーマ（第 5 章）を拡張する — プロンプトを作らない。
