# Demand Atlas — Review State

`REVIEW_ENGINE.md` の手順で毎回更新する。**レビュー本文は書かない。値と条件だけを保持する。**

---

## 更新規約（肥大化防止）

本ファイルは回数が増えても**行数がほぼ一定**であること。追記ではなく上書きで運用する。

| 対象 | 規約 |
|---|---|
| 前回測定値 | **上書きのみ**。時系列を残さない（推移が必要なら `history/` の実データを見る） |
| 領域ローテーション | 各領域 **1 行**。最新の発見のみ保持し、古い発見は上書きする |
| 自己評価履歴 | **直近 3 回のみ**。それ以前の行は削除する |
| 将来リスク | 発火済み・条件到達済みの項目は**削除**する（対応済みなら記録しない） |
| 保留中の改善案 | 実装したら**削除**する。却下は理由 1 行に圧縮する |
| 測定状況 | 測定済みリストは名詞の羅列に留める。詳細は「前回測定値」に置く |
| セクション | **むやみに増やさない**。追加時に ENGINE の同期は不要（O2） |
| 矛盾 | 同一ファイル内で矛盾する記述を残さない。検証・実装で前提が消えた項目は削除する（O8） |

判断に使わなくなった値は、記録として惜しくても削除する。

---

## 運用ルール（REVIEW_ENGINE.md への上書き）

**`REVIEW_ENGINE.md` は凍結する。以後編集しない。**
レビュー方法の改善は、ENGINE を書き換えず**このセクションに記録**する。

### 優先順位

本セクションと `REVIEW_ENGINE.md` の記述が矛盾する場合、**本セクションを優先**する。

### 現在の運用フロー

```
レビュー開始
  ↓
レビュー実施（ENGINE の PART A〜F）
  ↓
改善案を考える
  ↓
必要なら実装候補を出す（実装は 実装GO まで行わない）
  ↓
REVIEW_STATE.md だけ更新する
```

### 有効な上書きルール

変更履歴ではなく**現在有効なルールだけ**を保持する（更新規約に準拠）。

| # | 上書き内容 | 無効化する ENGINE の条項 |
|---|---|---|
| O1 | ENGINE は原則凍結。レビュー方法の改善は本セクションに追記する。**ただしモードの追加はユーザー指示により ENGINE 側に書く**（2026-07-30、`需要拡張` モードで解除） | 第 6 章「手順そのものを変える要望の場合のみ、本ファイルを改定する」 |
| O2 | STATE のセクション追加時に ENGINE のスキーマを同期しない | 更新規約「セクションを増やす場合は ENGINE 第 5 章も同時に更新する」 |
| O3 | 新しい観点・判定・評価軸は、ENGINE ではなく STATE の該当セクションへ追加する | 第 6 章 拡張規約の追加先指定 |
| O4 | **実装提案は「重大な問題」に限る。それ以外はレポートのみ**（下記定義） | PART E「即実装 最大 1 件」を毎回埋める運用 |
| O5 | **開発リソースは新機能・UX・パフォーマンスに集中**。監視・CI・コストは条件発生時のみ扱う | 巡回表の均等ローテーション |
| O6 | **レビューは問題発見で終わらない。毎回「次回レビュー計画」を自分で決定する**（下記 5 項目） | PART G が STATE 更新のみで完結する運用 |
| O7 | **実装したら効果を実測し、予測との差を記録する**。改善案が正しかったかまでレビュー対象にする | 実装で完了とする運用 |
| O12 | **`/work` は 1 応答で完結しない前提で運用する。** 開始時刻と終了予定を「/work の作業状態」に記録し、毎サイクルで進捗を上書きする。応答が切れたら同じ `/work` を再送すれば、残り時間を実測で計算して継続する。**`/compact` はこちらから実行できない**ため、状態保存で代替する | 「指定時間まで 1 回の応答で走り続ける」という前提 |
| O11 | **コマンドの正本は本ファイルの「コマンド一覧」**。ENGINE 側は手順の分岐だけを持ち、コマンドの追加・削除では改定しない。**一覧と実際の動作が食い違う状態を禁止する**（2026-07-30、ユーザー指示） | ENGINE 第 7 章に入力表を持つ運用 |
| O10 | **「需要拡張」の 1 語で Demand Expansion モードを実行する**（ENGINE 第 3.5 章）。このモードは分析→候補収集→重複除去→優先順位→実装→検証→STATE 更新までを一連で行い、**`実装GO` を待たない**。ただし追加は 1 回 1 件まで、STEP 7 の 4 条件を全て満たす時のみ | 「実装は `実装GO` が出たものだけ」（R4）をこのモードに限り上書き |
| O9 | **前回レビューから PROD の `generatedAt` と `HEAD` が両方不変なら、PART A は「差分なし」と 1 行で記録し、深掘りしない**。その回の PART B は「測定状況／未測定」から選ぶ（実測: 2026-07-30 に 10 分間隔でレビューが 2 回走り、PART A から新情報が 1 つも出なかった） | PART A を毎回フルに実行する運用 |
| O8 | **毎回 STATE 自身を監査する**。不要になったルール・役目を終えた監視項目・フェーズに合わない判断基準・標準化すべき知見を評価し、整理・統合・削除する。**削除は「今後も不要」と実測で言える場合のみ** | STATE を追記専用の記録として扱う運用 |

### レビューの締め（O6）— 毎回必ず答える 5 項目

1. 今回の目的は達成できたか
2. 今回のレビューで得られた知見
3. その知見によって次に調査すべきこと
4. 次回レビューで最優先となるテーマ
5. 次回の起動方法 → **常に「レビュー」の 1 語**。何を実行するかは本ファイルの「次回レビュー計画」が決める
   （プロンプトは再生成しない = R11 を維持したまま、ユーザーに次を考えさせない）

### 実装の事後検証（O7）

実装した改善は次回レビューで必ず以下を実測し、外れた場合は**予測の癖**を自己評価履歴に残す。

- 期待していた効果 / 実際の効果 / 副作用 / 運用コスト / レビュー時の予測との差

### 「重大な問題」の定義（O4）

PART A の異常判定に該当した場合のみ実装を提案する。それ以外は報告に留める。

- 本番が表示されない / console error > 0 / カード枚数と themes が不一致
- PROD age > 48h（更新が止まっている）
- themes が減少（データ欠落）
- pipeline が停止している（Actions failure / cancelled）
- ユーザーに見える誤った数値（飽和・破綻表示など実測できるもの）

上記に該当しない改善（保守性・リファクタ・監視強化・将来対策）は、
**発見しても記録のみ**とし、実装提案は行わない。

### コマンド一覧（正式版・唯一の情報源）

**ユーザーが追加を指示したコマンドだけを載せる。** 内部処理や通常機能は載せない。
このセクションと実際の動作が食い違う状態を禁止する。

| コマンド | 説明 |
|---|---|
| **コマンド** | 現在登録されているコマンド一覧を表示する。表示のみで、他の処理は行わない |
| **レビュー** | Autonomous Review を実行する。現在の状態を分析し、問題点・改善点・ボトルネック・次にやるべきことを自ら調査・検証して、必要なら本ファイルを更新する |
| **需要拡張** | Demand Expansion を実行する（ENGINE 第 3.5 章・STEP 1〜10）。**最終目標は、追跡できる需要・情報源・分析量・情報量を増やし続けること。** 需要テーマだけでなく**情報源・カテゴリ・分析方法・比較方法・可視化・データ**も情報量として扱う。コマンド自身のレビューと改善から始め、候補探索・**既存情報源が限界なら新しい公開データ / RSS / API / DB / 論文 / オープンデータの自律調査**（STEP 2.5）・8 軸評価・アルゴリズム改善・2 段階のテーマ追加・UI 全面への反映・整合性確認・動作検証・STATE 更新・一覧更新まで一連で行う。毎回**「前回より情報量が増えたか」を 11 指標の数値で評価**し、増えていなければコミットしない。追加したものは**サイト・データ・UI・README・STATE・コマンド一覧**へ反映し、ユーザーが利用できる状態まで完了させる |
| **アクセス分析** | 「今日訪れた人」を**実際に数えられている状態**まで仕上げる（ENGINE 第 3.7 章・STEP 1〜7）。現状を実測 → 不足を一覧化 → **自分で出来ることは全て自動実行**（修正・ビルド・検証・README / STATE / 一覧更新・本番確認）→ **アカウント操作だけが残ったら操作手順だけを表示して停止** → 「完了」を受けたら自動で続きを実行。重大な問題が 0 件になるまで終わらない。数えられていない時に推定値を出すことは禁止 |
| **実装GO** | 本ファイルの「即実装（GO 待ち）」に載っている項目だけを実装し、検証して本ファイルを更新する。空の場合は実装せず報告のみ |
| **/work 時間 作業名** | 指定時間の間、その作業だけを自律的に繰り返す（レビュー → 改善 → 実装 → 検証 → レビュー）。**停止条件は時間だけ**で、途中で確認を求めない。関係ない改善点は本ファイルに記録して元の作業へ戻る。例: `/work 30分 レビュー` `/work 2時間 需要拡張` |
| **/auto** | 自律実行モード。**後ろに続けた指示**を、確認を求めずに調査・実装・検証まで自走する（例: `/auto 新機能を考えて`）。下記「確認が必須な操作」に該当する場合のみ必ず確認する |

### 「コマンド」の表示書式（ユーザー指定）

```
【利用可能なコマンド】

・レビュー
説明...

・需要拡張
説明...
```

登録されているコマンドだけを、この形式で列挙する。未登録のものを例示として混ぜない。

### `/auto` で確認が必須な操作（ユーザー指定・自走しない）

自律実行モードでも、次に該当する操作は**必ず事前に確認する**。

- 課金が発生する操作
- API キーや認証情報の変更
- 外部サービスとの契約
- データベースの破壊的変更
- データ削除
- セキュリティポリシーの変更
- 法律・利用規約に関わる操作
- ユーザーの目的や要件を変更する提案

### 実行ルール

- **その単語だけが送信された時に実行する。** 通常の会話の中に出てきても実行しない
- **例外は `/auto` のみ**。`/auto` はメッセージに含まれていれば発動し、後続の指示文とセットで使う
- 一覧に無い語は実行しない（勝手に機能を増やさない）
- コマンド実行時は追加の指示を求めない。何をするかは ENGINE と本ファイルが決める

### 更新ルール

ユーザーからコマンドの追加・変更・削除の指示があった場合、次の 3 点を必ずセットで行う。

1. 機能を実装する（または変更・削除する）
2. 本セクションの一覧を更新する
3. 説明を 1〜3 行で簡潔に書く

`実装GO` の対象は、**その時点で「即実装」に載っている項目に限る**。
条件付き・将来検討・却下の項目は対象外とする。
「即実装」が空の場合は実装せず、その旨を報告して Autonomous Review に留まる。

### 成果物

レビュー 1 回あたりの成果物は **`REVIEW_STATE.md` の差分のみ**。
次回プロンプトも、レビュー本文の保存も行わない。

---

## メタ

| 項目 | 値 |
|---|---|
| 最終レビュー日 | 2026-07-30（14 回目・/work 20 分 ウェブサイトの改善） |
| モード | Autonomous Review |
| フェーズ | ゼロユーザー段階（MVP 完成 / 獲得未着手） |
| 最優先目標 | 最初のユーザーへの到達 |
| 開発リソースの配分 | **新機能・UX・パフォーマンスに集中**。監視・CI・保守は条件発生時のみ（O5） |
| 最大ボトルネック | **共有しても効果を観測する手段が無い（analytics 未導入 / GitHub stars 0）**。共有経路の技術的欠陥（OG 画像）は 2026-07-29 に解消済み。検索は現コンテンツ量では投資対効果が成立しない |
| レビュー回数 | 37 |
| 月額コスト | **¥0**（Actions 無制限 / vercel.app 無料 / 外部 API 無料） |

---

## コア監視コマンド

### ① 生産側 + 本番データ

```bash
cd "/c/Users/Owner/Desktop/claude作業用/demand-atlas" && echo "uncommitted: $(git status --porcelain | wc -l) | HEAD: $(git rev-parse --short HEAD)" && curl -s "https://api.github.com/repos/RoLororo/-web-/actions/workflows/317423174/runs?per_page=3" | node -e "const j=JSON.parse(require('fs').readFileSync(0,'utf8'));console.table(j.workflow_runs.map(r=>({started:r.run_started_at,conclusion:r.conclusion,s:Math.round((new Date(r.updated_at)-new Date(r.run_started_at))/1000)})))" && curl -s "https://demand-atlas.vercel.app/data/demands.json" | node -e "const d=JSON.parse(require('fs').readFileSync(0,'utf8'));console.log('PROD age:',((Date.now()-Date.parse(d.generatedAt))/3600000).toFixed(1),'h | themes:',d.demands.length);"
```

### ② 消費側（本番の実描画）

`preview_start` で `https://demand-atlas.vercel.app` を開き、`localStorage.clear()` 後に
title / カード枚数 / 総転送 KB / DCL / console error / 横スクロール を取得する。

### 異常判定しきい値

| 観測 | しきい値 |
|---|---|
| Actions conclusion | success 以外（2026-07-28 以降の run が対象） |
| Actions 実行時間 | > 1200s |
| uncommitted | > 0 |
| PROD age | > 48h |
| themes | 前回から減少 |
| カード枚数 | themes と不一致 |
| console error | > 0 |
| 総転送 | > 250 KB |

### データ品質（PROD age が更新された時のみ実行）

```bash
cd "/c/Users/Owner/Desktop/claude作業用/demand-atlas" && node -e "
const d=require('./data/demands.json'),fs=require('fs'),dir='./history/current/';
const ch=d.demands.map(x=>x.change).sort((a,b)=>b-a);
const st=d.demands.reduce((m,x)=>{m[x.status]=(m[x.status]||0)+1;return m;},{});
const cat=d.demands.reduce((m,x)=>{m[x.category]=(m[x.category]||0)+1;return m;},{});
const mo=d.demands.map(x=>x._insights?.momentum?.score||0);
const dq=d.demands.map(x=>x._insights?.dataQuality?.score||0);
const cov=d.demands.map(x=>(x._wikipediaDetail?1:0)+(x._qiitaDetail?1:0)+(x._arxivDetail?1:0)+(x._appstoreDetail?1:0)+(x._githubDetail?1:0)+(x._ndlDetail?1:0));
let flat=0,total=0;const files=fs.readdirSync(dir).filter(f=>f.endsWith('.jsonl'));
for(const f of files){const r=fs.readFileSync(dir+f,'utf8').split('\n').filter(Boolean).map(JSON.parse);
 for(let i=1;i<r.length;i++){const a=r[i-1].sources||{},b=r[i].sources||{};
  for(const s of Object.keys(b)){if(s==='ndl')continue; // stock 指標は静止率の対象外
   const p=a[s]?.metrics?.volume,c=b[s]?.metrics?.volume;
   if(typeof p!=='number'||typeof c!=='number')continue;total++;
   if(p===0||Math.abs((c-p)/p*100)<1)flat++;}}}
const recs=files.map(f=>fs.readFileSync(dir+f,'utf8').split('\n').filter(Boolean).length);
console.log('history avg:',Math.round(recs.reduce((a,b)=>a+b,0)/recs.length),'| keys:',Object.keys(JSON.parse(fs.readFileSync(dir+files[0],'utf8').split('\n')[0])).join(','));
console.log('spread:',ch[0]-ch[ch.length-1],'| clamp:',ch.filter(x=>x===200||x===-80).length,'| status:',JSON.stringify(st));
console.log('momentum100:',mo.filter(x=>x===100).length,'| dq<40:',dq.filter(x=>x<40).length);
console.log('6src:',cov.filter(x=>x===6).length,'| 2以下:',cov.filter(x=>x<=2).length,'| news<5:',d.demands.filter(x=>x._matchingArticleCount<5).length);
console.log('flat:',(flat/total*100).toFixed(0)+'% | cat:',JSON.stringify(cat));
"
```

| 観測 | しきい値 |
|---|---|
| status 最頻ラベル比率 | >= 70% |
| change clamp（±200 / -80） | >= 3 件 |
| momentum score 100 | >= 3 件 |
| dataQuality < 40 | >= 3 件 |
| flat ratio | > 50% |
| 6 ソース揃い比率 | < 30% |
| カテゴリ使用数 | 前回から減少 |

---

## 前回測定値（2026-07-30）

### Git / CI

| 項目 | 値 |
|---|---|
| HEAD | `3052db6` |
| uncommitted | 0。`REVIEW_ENGINE.md` / `REVIEW_STATE.md` は **2026-07-30 に git 管理下へ入れた**（運用マニュアルであり、実装と同じコミットで整合を保つため） |
| 直近 run | **07-30 success 633s（timeout 修正後の初回）** / 07-29 success 518s / 07-28 success 521s |
| 既知の失敗 | 07-25 cancelled 618s / 07-27 cancelled 619s（**timeout 30min 修正前。該当扱いしない**） |
| timeout 修正の効果 | **検証完了**。07-30 の run は **633s = 旧 timeout 10 分なら確実に cancelled** だった。3 日連続成功（07-28 / 29 / 30） |
| **schedule の実配信時刻** | **cron 21:00Z に対し 8/8 が遅延**。中央値 +3.3h / 最小 +3.1h / 最大 +12.0h。JST 換算で最速 09:06・最遅 17:59。**JST 06:00 に走った実績は 0 回** |
| 欠測 | 8 日中 2 日（25%）。欠測した 2 回はいずれも遅延 9h 超の run |
| 成功時の実行時間 | min 362s / **max 633s** / avg 500s。しきい値 1200s に対し 53% |

### 本番

| 項目 | 値 |
|---|---|
| generatedAt | 2026-07-30T00:24:38.592Z |
| themes | 10 |
| カード描画 | 10 |
| 総転送（Home cold） | **163 KB を実測**（グラフ差し替えで +1 KB。内訳 JS 83 / CSS 15 / demands 47 / jsonl 14 / index 2 / doc 2） |
| 総転送（`/ideas` cold） | **144 KB / 3 リクエスト**（実測）。history を読まないぶん Home より軽い |
| データリクエスト | Home 15 / `/ideas` 3 |
| DCL | Home 1,170 ms（cold）|
| console error | 0 |
| 横スクロール | なし（375 / 1280） |
| local との同期 | 一致 |
| 前回レビューとの差分 | **なし**（レビュー #26 の 10 分後に #27 を実施。`generatedAt` / HEAD / run すべて同一 → O9 を新設） |

### データ品質

| 項目 | 値 |
|---|---|
| change | `[100,40,22,12,8,-8,-25,-41,-60,-69]` / spread 169 |
| clamp | **0 件**（±200 / -80 とも 0） |
| status | 急上昇 3 / 成長中 2 / 下降 5 |
| momentum score | `[80,55,51,40,40,25,25,10,10,10]` / 100 飽和 0 |
| momentum label | **加速 1** / 成長中 2 / 定着 2 / 減速 2 / 沈静化 3（初めて「加速」が出現＝閾値 change>=100 が到達可能と確認） |
| verdict | **拡大局面 1** / 定着 1 / 様子見 4 / 研究先行 2 / 認知拡大中 1 / 鎮静化中 1 |
| dataQuality | 63〜100 / < 40 は 0 件。**100 が 1 件**（上限到達、監視対象に追加） |
| カバレッジ | 平均 5.0 源 / 6 源揃い 3 / 2 以下 **0** |
| news < 5 件 | 3 テーマ |
| flat ratio | 40%（78/196）。内訳 **github 30%（最も動く）** / qiita 35% / wikipedia 38% / arxiv 47%。**NDL 単体は 100%（11/11）静止 → 除外していなければ全体は 43% に悪化していた** |
| カテゴリ | ビジネス 4 / AI・テクノロジー 5 / 健康 1 = **3 / 9 使用** |
| envelope complete・coverage | 24 観測すべて健全（異常 0） |

### テーマ拡張（2026-07-30 初測定）

| 項目 | 値 |
|---|---|
| 観測しているテーマ | **11**（5 つの mapping と wikipedia / insights / candidates 辞書に定義） |
| 公開されているテーマ | **10**。差の 1 件（`ai-content-generation`）は **7 世代すべての日次コミットで不在** |
| 非公開になる条件 | `extract-demand-candidates.mjs` の `if (evidence.length === 0) continue`。**猶予期間なし・記録なし・UI 表示なし** |
| 昇格基準の充足状況 | **3 / 10 が未達**（remote-work news 4 / home-server 3 / senior-health 3。基準は「3 ソース以上 かつ news 5 件以上」） |
| 1 テーマ追加に必要な編集 | **9 ファイル**（qiita / appstore / arxiv / github / ndl の mapping + `fetch-wikipedia-pageviews.mjs` + `generate-insights.mjs` + `extract-demand-candidates.mjs` + `themeCatalog.js`） |
| 発見の原材料 | 未紐付け記事 **607 / 691 件**。頻出語は事件・固有名詞が支配（熊本 20 / ジャンプ 19 / イラン 12 / 高市首相 12）。**一般ニュースの語頻度だけでは需要テーマが出ない** |
| 候補評価に使える既存資産 | `keyword-trends.json` = 任意キーワードの Google News RSS 集計（**57 語・API キー不要・日次系列つき**）。`totalItems` は **55/57 が 100 件上限に飽和**して使えないが、**velocity（件/日）は 0.4〜26.5 の 66 倍レンジ**で飽和せず判別力がある（中央値 1.7） |
| ニュース corpus の上限 | RSS **4 本**（NHK / ITmedia / Zenn / はてな）。日次 19〜92 件（中央値 約 68） |
| 記事の帰属 | 排他ではない（**13 / 84 件が 2 テーマ以上の根拠**）→ キーワード追加で既存テーマを壊す心配はない |
| 停滞テーマの原因 | `ai-content-generation` の hot 語 3 つ（動画生成 / 画像生成 / 音声合成）は 691 記事中 **すべて 0 件**。代替語も精度が低い（`生成AI` 16 件中、内容が合致するのは 3 件程度） |

### 情報量（2026-07-30 初測定）

| 項目 | 値 |
|---|---|
| demands 1 件のリーフ数 | 211（ユニーク末端キー 110） |
| UI が参照する末端キー | 68 → **69 / 110**（`byDate` を追加） |
| 完全に不可視（散文にも出ない） | **10 個** — `prior5Days` `rawRatio` `medianRatio` `totalPageviews7d` `totalPageviewsPrior7d` `ndl.bibliographyCount`(792) `github.newRepoCount`(54) `github.topLanguage`(HCL) `arxiv.uniqueAuthors`(219) `_dataQuality`(0.89) |
| 散文のみで可視（数値としては未表示） | 10 個 — `recent2Days` `relativeRatio` `_keywordTrendTotal` `qiita.articleCount/lgtmSum/uniqueAuthors` `github.topStarSum` `arxiv.paperCount/avgAuthorsPerPaper/primaryCategoryTop` |
| `trendData` 非ゼロ率 | 7d 26% / 30d 18% / 90d 6%（**2026-07-30 に主系列から外した**。副情報として注記に残している） |
| `_wikipediaDetail.byDate` | 29 日 × 非ゼロ率 100% × 10 テーマ。**`9694b88` で主系列に採用**（`src/utils/series.js`） |
| history の metric 種類 | volume / engagement / contributors / latestActivityAt（4 種） |
| Rankings が使う metric | **volume のみ**（`r.metric === 'volume'` でフィルタ） |
| Compare が出す判定 | score / change / status / momentum / beginner / competition。**verdict と dataQuality は無い** |
| ニュース | 取得 691 件 → テーマ紐付け **97 件（14%）**。1 テーマ 16 件中 UI 表示は evidence 8 + problems 5 |
| 日次系列を作れる情報源 | wikipedia のみ実装済。**qiita / arxiv / github は全項目を走査済みなので API 追加なしで日次化可能**（topItems を 5 件に絞る前に集計すればよい）。appstore は日付フィールドを持たず不可、ndl は年単位のため不可 |

### サイズ

| 項目 | 値 |
|---|---|
| demands.json | **47 KB gzip / 267 KB raw** |
| history | 89 KB(raw) / 77 レコード / **1,183 B per record** |
| history jsonl（1 テーマ・7 レコード） | 約 8 KB raw |
| .git | 3.9 MB |
| data/ | 888 KB |
| public/ | 327 KB |
| bundle JS | **257.3 KB / gzip 82.5 KB** |
| bundle CSS | **75.1 KB / gzip 13.2 KB** |
| mockDemands（lazy） | 11 KB / gzip 7.5 KB |

### history

| 項目 | 値 |
|---|---|
| avg records | 7 |
| 期間 | 2026-07-22 〜 07-30（distinct 7 日、07-25 / 07-27 欠測） |
| record keys | `date, generatedAt, sources`。判定結果は未保存だが **git に demands.json の日次スナップショットが 22 世代あり代替可能**（下記 逆算チェック） |
| archive ディレクトリ | 本番では未生成（retention 90 日に未到達）。**動作は検証済み**（2026-07-29） |

### SEO / セキュリティ

| 項目 | 値 |
|---|---|
| 初期 HTML の本文 | **27 文字**（script/タグを除去して計測。2026-07-30。**未解決**） |
| 内部リンク（非 JS クローラー視点） | **0**（**未解決**。SSR / プリレンダを入れない限り変わらない） |
| robots.txt | **解決**（2026-08-01）。`public/robots.txt` を設置。`public/` は rewrite より先に配信されると実測（`og-image.jpg` が `image/jpeg` で返ることで確認） |
| sitemap.xml | **解決**（2026-08-01）。`scripts/generate-sitemap.mjs` が prebuild で生成。**27 URL**（固定 13 + テーマ 10 + 分野 3、`/favorites` は除外）。`lastmod` は `generatedAt` |
| 各 URL の title / description | **解決**（2026-08-01）。`useSeo` で 18 ルート全てに固有の title / description / canonical を設定。JS 実行後に実測済み（下表） |
| canonical | **解決**（2026-08-01）。全ページに設定。`noindex` のページには**出さない**（矛盾指示の回避。`/this-does-not-exist` が自分自身を canonical にしていたのを修正） |
| 構造化データ | **解決**（2026-08-01）。WebSite / BreadcrumbList / Dataset / FAQPage / TechArticle / AboutPage / ContactPage。実測前は **0 件** |
| soft 404 | **緩和**（2026-08-01）。HTTP は 200 のまま（静的ホスティングの制約）だが `noindex, follow` を出す |
| 詳細ページの見出し | **解決**（2026-08-01）。本文 5,806 字に対し h1 が 1 個だけで h2 が 0 だったのを、`block-title` 15 個を `<h2>` 化（見た目は不変） |
| パンくず | **解決**（2026-08-01）。詳細ページ・分野ページ・説明ページに設置（+ BreadcrumbList） |
| フッターの内部リンク | **解決**（2026-08-01）。`<a href>` 5 本を `<Link>` 化（毎回ページ全体を再読み込みしていた）。サイト情報 5 本を追加 |
| meta description の内容 | **是正済み**（`c9e8e90`）。実際の 7 情報源を列挙する文へ変更し本番で確認 |
| OGP タグ | og:url / og:image / twitter:image とも絶対 URL（修正済） |
| OG 画像 | **`og-image.jpg` を配置し解決**（1200x630 / 44,331B / `image/jpeg`・本番でバイト一致を確認） |
| OG 画像の**内容** | **2026-07-30 に初めて目視確認し問題なし**。ロゴ / LIVE バッジ / 見出し「今、世の中で何が求められているのか。」/ 補助文「需要の変化から、次のビジネスチャンスを発見する。」/ 上昇曲線。誤った情報源の記載なし。見出しは canvas 高の 14% で、500px 幅に縮小しても可読 |
| 計測手段 | **ゼロ**（analytics 未導入 / GitHub stars 0・watchers 0・forks 0） |
| npm audit（prod） | react-router moderate × 2（open redirect / SSR hydration）**実害未検証** |

---

## 広告掲載（AdSense 審査）の状態（2026-08-01 実測・2 回目の改善後）

### 実装済み

| 項目 | 状態 | 実測 |
|---|---|---|
| プライバシーポリシー | `/privacy` | 10 節。localStorage 5 キー・アクセス集計・**問い合わせ内容の扱い**を実装どおりに記載 |
| 利用規約 | `/terms` | 10 節 |
| お問い合わせ | `/contact` | **サイト内フォームで完結**（`/api/contact`）。外部サービス不要。テスト 25/25 通過 |
| 運営者情報 | `/about` | できないこと 5 項目を明記 |
| 計算方法 | `/methodology` | FAQPage + TechArticle |
| 読み方ガイド | `/guide` | HowTo 構造化データ。例に使うテーマは demands.json から取るので古びない |
| 情報源の解説 | `/sources` + `/sources/:id` × 7 | **取れなかったテーマと理由をそのまま公開** |
| 用語集 | `/glossary` | FAQPage（用語 10 + 判定 7） |
| robots.txt | 実ファイル | AdsBot-Google を明示 Allow。**`/data/` `/history/` は Disallow しない**（塞ぐと SPA の本文が全部見えなくなる） |
| sitemap.xml | 生成 | **37 URL**（固定 16 + テーマ 10 + 情報源 7 + 分野 3 + 除外 `/favorites`） |
| 404 | `noindex, follow` | HTTP は 200 のまま（静的ホスティングの制約） |
| OGP / meta / canonical | ページ別 | **29 ページで重複 0**（title / description / canonical すべて） |
| 構造化データ | 19 ページ | WebSite / BreadcrumbList / Dataset / FAQPage×2 / TechArticle / HowTo / AboutPage / ContactPage |
| 広告枠 | 5 か所・未有効 | `ADS_ENABLED = false`。本番バンドルに `ad-slot` の文字列が**存在しない**ことを確認 |

### 今回の実測（375px・ライトテーマ・29 ルート）

| 項目 | 前回 | 今回 |
|---|---|---|
| 独自テキスト合計 | 25,589 字 / 16 ページ | **44,388 字 / 29 ページ**（平均 1,531 字） |
| 索引対象で 600 字未満のページ | 4 件（最小 377） | **0 件**（最小 720） |
| 内部リンク 0 のページ | `/compare` `/explore` `/categories` | **0 件** |
| タップ対象 24px 未満（WCAG 2.5.8 AA） | 342 | **1**（404 ページのみ・noindex） |
| title / description / canonical の重複 | 0 | **0** |
| 横溢れ | 0 | **0** |
| console error | 0 | **0** |
| コントラスト違反（2,350 要素・アルファ合成込み） | — | **0** |

### 今回直した重大・中の問題

| 段階 | 内容 |
|---|---|
| 重大 | **需要カードと分野カードが `<button onClick>` で、`<a>` が 1 本も無かった。** クローラーは詳細ページ（1 ページ 4,692 字・サイト最厚）へ内部リンクで到達できず、キーボードでも開けなかった。実測: Home の詳細ページへのリンク 0 → **14 本**、Explore 0 → 10 本、Categories 0 → 3 本 |
| 中 | `/categories` 377 字・`/compare` 429 字（内部リンク 0）と、索引対象なのに中身が薄いページがあった → 説明を追加し 720 字以上に |
| 中 | 検索欄の `<input>` が 22px しかなく、枠（48px）の上下をタップしても入力に入らなかった → 枠の padding を入力側へ移し 34px に（枠は 48→46px） |
| 軽微 | `sourceGuide.js` の文字列内の `**` がそのまま画面に出ていた |

### 計測側の誤りを 2 件訂正（サイトの不具合ではない）

- `rgba(80,200,120,0.12)` を不透明として扱い `.chip-btn` を 3.10:1 と誤判定していた。
  アルファ合成すると **5.58:1** で適合。→ 判定器にレイヤ合成を実装
- `background-clip: text` のグラデーション見出し（`.accent`）を 1.00:1 と誤判定。
  実際は両端とも 5.6:1 以上。判定器では評価できない

### プリレンダ（2026-08-01 実装・本番反映済み）

`vite build` の後に `scripts/prerender.mjs` が走り、37 ルートぶんの
`dist/<ルート>/index.html` を書き出す。Vercel はディレクトリ index を
静的ファイルとして解決するため、rewrite より先に配信される（本番で実測）。

| | 実装前 | 実装後（本番実測） |
|---|---|---|
| 生 HTML の title | **37 URL すべて同一** | **全て固有** |
| 生 HTML の og:title | 同上 | 全て固有 |
| 生 HTML の本文 | **251 字**（全ページ） | **341〜11,045 字** |
| 生 HTML の内部リンク | **0 本** | **20〜30 本** |
| canonical | なし | 全 37 ルート |
| 構造化データ | なし | 29 / 37 |
| CLS（本番） | — | **0** |
| console error（本番） | — | **0** |

意味: X・Slack・LINE・Facebook のカードは JavaScript を実行しない。
実装前はどのテーマを共有しても同じ見出しが出ており、SNS 流入が
構造的に成立しなかった。検索側も描画待ちの列に入るぶん反映が遅れていた。

#### 実装中に出た 2 つの問題

| 問題 | 対応 |
|---|---|
| `renderToString` は Suspense を待てず、React.lazy の 9 ページが**フォールバックのまま**出力された（/sources /guide /glossary が本文 746 字・title 既定値・canonical 欠落） | `renderToPipeableStream` + `onAllReady` に変更 |
| React の Node ストリームがチャンク境界で **NUL バイト**を混ぜてきた。完全な UTF-8 文字の**あいだ**に入る（`e3 81 8a`"お" → `00` → `e3 82 8a`"り"）。1 ファイルあたり 1〜6 個 | entry-server で除去。prerender 側で U+FFFD を検出したらビルドを落とす（本物の文字を巻き込んでいないことの担保） |

#### 設計判断

- `main.jsx` は `hydrateRoot` にせず `createRoot` のまま。
  AnimatedNumber と日時表示はサーバーと必ず食い違うので、
  不一致を合わせに行くより描き直す方が壊れにくい。
  実測で CLS 0 なので、描き直しによるガタつきは発生していない
- `useSeo` に SSR 用の収集器を足した。効果はサーバーで走らないため。
  ブラウザでは収集器が null なので**動作は完全に従来どおり**

#### 残っている薄いページ

`/changes` `/timeline` `/rankings` `/whats-new` は `history/*.jsonl` を
効果の中で読むので、サーバーでは空の状態が描画される（生 HTML で 341〜1,191 字）。
クライアントと Google の描画では埋まるが、生 HTML では実力より薄く見える。
直すには historyService をレンダー中に読めるようにする必要がある。

### 審査を阻むもの（未解決）

| # | 項目 | 内容 |
|---|---|---|
| B1 | **年齢要件** | AdSense の利用規約は 18 歳以上。**申請はこれが満たされるまで行わない**（本人の方針） |
| B2 | トラフィック | 訪問者 1 桁（2026-08-01: 今日 3 / 昨日 7 / 累計 10）。検索の反映待ち |
| B3 | ads.txt | 審査通過後、パブリッシャー ID が出てから設置 |
| B4 | 生 HTML が薄い 4 ページ | 上記のとおり history 由来。優先度は中 |

**解決済み**: 本番未反映（2026-08-01 push 済み）／初期 HTML 27 字（プリレンダで解決）。

### バンドルサイズ

| | 改善前 | 現在 | 差 |
|---|---|---|---|
| index.js (gzip) | 85.33 KB | 91.17 KB | +5.84 KB |
| CSS (gzip) | 14.35 KB | 15.6 KB | +1.25 KB |
| 説明ページ 9 本 | — | 別チャンク（初回表示に載らない） | — |

---

## 領域ローテーション

**ユーザー価値に直結する領域を優先**（O5）。下段は条件発生時のみ扱う。

### 主ローテーション（毎回 1 領域）

| # | 領域 | 実施日 | 主な発見 | 次に見る切り口 |
|---|---|---|---|---|
| 1 | 新機能候補 | 2026-07-30 | **アイデア一覧 `/ideas` を実装**（80 件を横断表示）。データ・pipeline 変更ゼロで実現。次点は「今週の観測ハイライト」 | ハイライトの dedup 方式（同一ニュースが複数テーマに重複） |
| 2 | UX・初訪問体験 | 2026-07-29 | **モバイル実測確定**: Hero が画面の 58%・fold 内コンテンツリンク 1 本・総高 3202px(4画面)。デスクトップは 3 本で問題なし | 実ユーザーの離脱データが取れた時 |
| 3 | パフォーマンス（体感） | 2026-07-30 | 総転送 **162KB** / 15req / DCL 1,170ms(cold)。history が 1 日約 2.0 KB 増える | 250KB 到達時に履歴読み込みを制限 |
| 4 | 表示数値の納得感 | 2026-07-30 | score 内訳は式と一致し飽和なし（成分 1.0 飽和は newsVolume 3/10 のみ）。**「情報源多様性」がページ下部の「情報源」と別物なのに同名で、誤読必至だった**→ ニュース媒体の多様性へ改名 | momentum の固定閾値（>=100）が相対指標 change と噛み合うか |
| 9 | **テーマ拡張（追跡数）** | 2026-07-30 | **発見の仕組みは存在しない**（`extract-demand-candidates` は手書き 11 テーマ辞書との照合）。候補の保存先は `demand-candidates.json` があるが状態も履歴も持たない。**昇格は 9 ファイルの手編集**。降格は `evidence.length === 0` で無言・即時に発生し、`ai-content-generation` は **8 日連続で非公開**。逆に昇格基準未達の 3 テーマ（news 3〜4 件）が公開され続けている | 候補レジストリ導入後の昇格・降格の実発生数 |
| 8 | **情報量** | 2026-07-30 | **表示グラフ「需要の変化」の非ゼロ率は 7d 26% / 30d 18% / 90d 6%**（ニュース件数由来・y 軸上限 5・2 テーマは 7 日全ゼロ）。一方 `_wikipediaDetail.byDate` に **29 日 × 100% 非ゼロ**の系列が全 10 テーマ分あり UI 参照 0。ほかに完全不可視の指標 10 個、散文のみ 10 個、Rankings は volume 固定（engagement / contributors 未使用）、Compare に verdict・dataQuality なし、ニュース 691 件中テーマ紐付けは 97 件（14%） | グラフ差し替え後の情報密度 |
| 7 | コード品質 | 2026-07-30 | **lint / test 設定は存在しない**（devDeps 4 個のみ / `npm test` なし）。src 5,257 行・最大 `DemandDetail.jsx` 507 行。`console.log` 残骸 **0**。**未使用 export 7 個**（うち `getCategories` は今回の変更で発生）。ただし tree-shaking で bundle には**含まれない**（`timeAgo` の固有文字列「分前」が dist に 0 件）ためバイト影響ゼロ | テストを入れる価値が出る条件（第三者参加 or 回帰の実発生） |
| 5 | アクセシビリティ | 2026-07-30 | **13 ルート中 4 つに h1 が無く、検索欄に名前が無かった**（修正済）。コントラストは全て 6.5:1 以上、フォーカスリングはブラウザ既定が有効（実キー入力で確認） | 読み上げ順序、ランドマーク |
| 6 | SEO・獲得 | 2026-07-30 | description の誤記述を是正済。**OG カードの中身を初めて目視し問題なし**（ただし具体的な数字は 1 つも入っていない） | 共有が発生した後のカード実表示 |

### 低頻度（条件発生時のみ）

| 領域 | 最終実施 | 状態 | 再訪する条件 |
|---|---|---|---|
| 運用・監視 | 2026-07-29 | `npm run check` 導入済 | 通知設定の確認結果が出た時 |
| コスト | 2026-07-29 | 月額 ¥0 / 帯域上限 80 万訪問 | 収益化に着手する時 |
| セキュリティ | 2026-07-29 | react-router moderate × 2（実害未検証） | 実害が確認された時 |
| ドキュメント・開発フロー | — | 未実施 | 第三者が開発に参加する時 |
| API 準備 | — | 未実施 | API 公開を決めた時 |

---

## 将来リスク

### 発火予定（日付確定）

| 時期 | 事象 |
|---|---|
| **2026-09-13 頃** | Home 総転送が **250 KB 超**。実測 162 KB（固定部 146 KB + history 16 KB / 7 日）から、history 1 日あたり約 2.0 KB として残り 44 日 |

archive ローテーションは 2026-07-29 に `HISTORY_RETENTION_DAYS=3` で実測検証済み
（66 行 → current 33 + archive 33、損失 0・重複 0・冪等）。リスク解消のため発火予定から除外。

### 数値条件

| 条件 | 現在値 | 到達時の対応 |
|---|---|---|
| Home 総転送 > 250 KB | **163 KB 実測**（65%） | 履歴読み込みの範囲を制限。増加分は history のみ（約 2.0 KB/日） |
| demands.json > 150 KB gzip | **47 KB**（31%） | summary / detail 分割 |
| 月間訪問 > 40 万 | 実測不能 | Hobby 100GB 超過。実測 131 KB/訪問で上限 **80 万訪問**、90 日充填後（273 KB）は **38 万訪問** |
| テーマ 100 到達 かつ 月間訪問 > 5 万 | テーマ 10 | 100 テーマ時は 1.9 MB/訪問で上限が **5.5 万訪問**まで低下。テーマ数と訪問数が乗算で効く |
| 収益化の着手 | 未着手 | **Vercel Hobby の商用利用可否を規約で確認**（不可なら Pro $20/月へ移行が必要） |
| Actions run > 1200s | **最大 633s**（53%） | timeout 30 → 60 min |
| history avg records >= 30 | 7 | momentum 閾値を再校正。**2026-07-30 に「加速」が初到達したため、閾値が原理的に届かない疑いは解消** |
| テーマ追加が月 3 回以上 | 0 | テーマ定義を `config/themes.json` へ移行 |
| history > 500 MB | 113 KB | Turso driver 実装 |
| .git > 300 MB | 3.9 MB | R2 driver 実装 |

### 仮説（未検証）

- モバイル fold のコンテンツリンク 1 つが離脱を招く（**fold の構造は実測確定**、離脱への影響のみ未検証）

- テーマ追加日に全テーマの change が動く（中央値正規化のため）
- momentum の固定閾値（>= 100）と相対指標 change の不整合

---

## 逆算チェック

| 計画中の機能 | 必要なデータ | 取得状況 |
|---|---|---|
| verdict / status transition の可視化 | 過去の verdict・status | **git の日次コミットに 22 世代あり取得可能**（2026-07-30 に実測。`git show <sha>:data/demands.json`） |
| momentum 閾値の再校正 | 過去の change / momentum | 同上。ただし**比較可能なのは 2026-07-29 の中央値正規化（`0b18d5a`）以降**。それ以前は算出式が違うため接続不可 |

**前回までの「未保存 → 実装不可」は誤りだった**（2026-07-30 に訂正）。
`data/demands.json` は日次コミットされており、判定結果は git 側に残っている。
失われているのは値そのものではなく**算出式をまたいだ比較可能性**。
history record への 5 フィールド追加（+7%）は「アプリ実行時に過去判定を読む」場合にのみ必要で、
分析目的には git で足りる。着手条件は「最初のユーザーを観測できた時」で変更なし。

---

## 保留中の改善案

### 即実装（GO 待ち）

**なし。** テーマレジストリと評価スクリプトは実装済み（O8 に従い削除）。

### 情報源の調査結果（再調査不要）

| 候補 | 判定 |
|---|---|
| NDL Search | **実装済**（d969777）。11/11・定着度カテゴリを充足 |
| OpenAlex | 保留。9/10 だが tech テーマで arXiv と重複。**非 tech テーマに限定マッピングすれば重複は解消する**（senior-health 17件/月のフロー）。ただし対象が実質 1 テーマのため優先度低 |
| J-STAGE | 保留。10/10・日本語学術だが日付フィルタが**年単位**でフローにならず、「研究」カテゴリが arXiv と重複 |
| EDINET | 除外。**401 subscription key 必須**（絶対条件違反） |
| Wikipedia 編集回数 | 除外。認知症で 30 日 4 件と過疎、信号として薄い |
| PubMed / Crossref | 除外。OpenAlex の部分集合・上位互換関係 |
| Hacker News | 除外。英語圏のみで日本市場に非適合 |
| Stack Overflow | 除外。技術限定（dementia で 0 件）、GitHub と同じ弱点 |
| Google Books | 除外。キーなしで Quota exceeded |
| connpass | 除外。403、API キー必須化 |
| はてなブックマーク | 除外。既存ニュース RSS に含有 |
| Google Trends / Reddit / YouTube / 楽天・Amazon / e-Stat | 除外。ToS 違反・API 制限・キー必須 |

### 条件付き実装

| 案 | 条件 |
|---|---|
| history スキーマ拡張 | 最初のユーザーを観測できた時 |
| sitemap.xml + ページ別 meta | 検索を獲得チャネルに加えると判断した時。**2026-07-30 再測でも初期 HTML 本文 27 文字 / a タグ 0 のままで、状況は不変** |
| モバイル Hero 圧縮 / fold 導線追加 | 実ユーザーの離脱データが取れた時 |
| Slack / Issue 自動起票による通知 | GitHub の通知設定が無効と判明した時。有効なら不要 |
| 劣化を UI に露出（dataQuality へ complete/coverage を反映） | 見るユーザーが存在するようになった時 |
| fetcher の retry | 同一情報源で `errorCount > 0` が **3 日連続**で観測された時 |
| npm audit fix | Compare の入力が `<Link to>` に流れると確認できた時 |
| 未使用 export 7 個の削除 | **専用の作業はしない**。該当ファイル（`format.js` / `demandService.js` / `historyService.js` / `sourceCatalog.js`）を次に別件で触る時に同時に削除する。bundle 影響はゼロなので急がない |
| lint / test の導入 | 第三者が開発に参加する時、または同じ回帰を 2 回踏んだ時 |
| **Rankings に engagement / contributors 軸を追加** | history avg records **>= 14**（現在 7）。2 週間未満では日次の上下が大きく順位が毎日入れ替わるため |
| **Compare に verdict / dataQuality を追加** | 次に `Compare.jsx` を別件で触る時に同時実施（単独では着手しない） |
| **qiita / arxiv / github の envelope に `byDate` を追加** | 上記グラフ差し替えが入り、複数系列を重ねる価値が確認できた時。**API 追加コストはゼロ**（全項目を既に走査済み）だが demands.json が +2〜3 KB gzip 増える見込み |
| **完全不可視の 10 指標のうち意味のある 4 つを Detail に出す**（NDL 書誌 792 / GitHub 新規 repo・主要言語 / arXiv 著者数 / Wikipedia 7 日窓） | グラフ差し替え後、Detail の情報密度に余裕があると実測できた時 |
| **未紐付けニュース 607 件（88%）の活用** | 「今週の観測ハイライト」の dedup 方式を決めた時 |
| **テーマ定義を `config/themes.json` へ集約**（9 ファイル → 2 ファイル） | **次にテーマを 1 件でも追加すると決めた時**。追加のたびに 9 箇所を手編集する構造が拡張の律速になっている |
| **ニュース RSS の本数を増やす**（現在 4 本） | 候補発見を始めると決めた時。**新しい API ではなく既存 rss-parser の追加インスタンス**なので絶対条件に触れない。日次 68 件・4 媒体が発見の上限を決めている |
| **velocity による候補キーワードの種出し** | `evaluate-themes.mjs` が入った後。velocity 上位語は「AI / 情報 / 仕事 / 開発」と汎用語に偏るため、**特異度フィルタの設計が前提** |
| Home 履歴読み込みの範囲制限 | Home 総転送 > 250 KB |
| demands.json 分割 | > 150 KB gzip |

### 将来検討

**新機能候補（既存データのみで実装可能・2026-07-29 実測）**

| 候補 | 成立性 | 備考 |
|---|---|---|
| 今週の観測ハイライト（横断フィード） | ○ | 直近 7 日で 62 件。要 dedup（同一ニュースが複数テーマに重複）。**アイデア一覧の次に有望** |
| 同一ニュース共有テーマの関連表示 | ○ | 重複を実測済み。訴求力は低い |
| 3 テーマ以上の比較 / カテゴリ横断の比較 | 判断材料不足 | Compare は 2 テーマ固定。3 つ以上を求める声を観測する手段が無い（analytics 未導入） |
| アイデアの保存 / メモ | 判断材料不足 | 使われているかを測る手段が無い（analytics 未導入）。**実ユーザー観測まで着手しない** |
| **判定が変わったテーマの通知**（status / verdict の遷移を Home に出す） | ○ | **2 つの独立した窓で実測（2026-07-30）**<br>9.5h（中央値正規化後）: change 10/10・score 7/10・status **1/10**・verdict **1/10**・順位 2/10<br>24.0h（旧式・07-28→07-29）: change 6/10・score 9/10・status **2/10**・verdict **1/10**・順位 3/10<br>→ **算出式が違っても verdict は 1/10 で一致**。判定は 1 日で 1〜2 テーマしか動かない。現在の Home「動いた 3 テーマ」は生の volume 変化しか見ておらず、**判定の遷移は表示されていない**。再訪の動機として最も筋が良い候補 |

アイデア一覧ページは 2026-07-30 に実装済み（`96a2c59`）。

**制約（実測）**: 新規コンテンツは 1 日 5〜10 件しか増えないため、日次フィードは成立しない。
数値は日次で変わる（60% の観測点が 1% 以上変動）が、コンテンツの自然な粒度は週次。

**その他**: アナリティクス導入 / プリレンダリング / テーマ定義の config 化 / momentum の percentile 化 / Turso・R2 driver / 帯域監視

### 却下

| 案 | 理由 |
|---|---|
| 現時点でのプリレンダリング | テーマ 10・データ 7 日では投資に見合う検索順位が取れない |
| UI の先回り改修 | ユーザー不在では効果を検証できない |
| generate-insights.mjs（909 行）の分割 | 触る頻度が低く、抽象化のための抽象化 |
| DemandDetail.jsx（504 行）の分割 | 順序変更のしやすさを失う |
| mockDemands の完全削除 | dynamic chunk で bundle に影響なし、障害時保険として無害 |
| storage の追加抽象化（entity 層など） | 現 interface で DB / R2 双方に mappable |
| `ai-content-generation` の hot 語に `生成AI` を足して公開する | **精度が足りない**。16 件ヒットのうち内容が合致するのは 3 件程度（残りは大学教授・クオンツ・新卒研修など）。語の問題だけでなく **corpus 自体に信号がない**（`画像生成` `動画生成` `音声合成` `Stable Diffusion` `Midjourney` `Sora` すべて 0 件） |
| `analyze-themes.mjs`（LLM によるテーマ生成）を自動化経路に使うこと | **`ANTHROPIC_API_KEY` 必須**。絶対条件「API キー原則不要」に反するため、無料運用では選べない |
| 90 日レンジのタブを維持すること | 実データの非ゼロ率が **6%**。原理的に存在しない期間を選ばせていた（`9694b88` で除去済み） |
| OG カードに具体的な数字を焼き込む（テーマ数 / 情報源数 / アイデア数） | カードは依存ゼロで作るため**手作業でのブラウザ生成しか手段がない**。情報源数は 2 週間で 4→5→6→7 と 3 回変わっており、焼き込めば同じ回数の手作業と陳腐化リスクが発生する |

---

## /work の作業状態（O12）

`/work` 実行中はここを**毎サイクル上書き**する。文脈が失われた場合（環境側の要約・リセット）はここから再開する。

| 項目 | 内容 |
|---|---|
| 目的 | **ウェブサイトの見た目の改善**（`/work 5時間制限が来るまで ウェブサイトの改善（主に見た目）`）— **終了**。対象は視覚品質のみ = 余白・階層・タイポ・密度・整列・レスポンシブ |
| 開始 / 終了 | 2026-07-30T11:12:05Z → 12:13Z（**約 61 分でユーザーの 5 時間制限に到達し終了**）/ 15 サイクル |
| 完了済み（UI 12 コミット） | `a4c31f8` カードのグリッドが 4 列なのに子 5 個で、スコアのピルが 2 行目 38px 幅に落ち **x=-8 まで画面外**（148→119px・均一・はみ出し 3→0）<br>`c849488` 説明文の 1 行を 46 全角に制限（実測 98/92/87/81/70/66 文字・max-width 指定 0 件）<br>`605ce6a` 11px 未満の CSS 25 箇所を 11px に。グラフの軸ラベルを SVG→HTML（横 89%/41% の潰れを解消・目盛りのズレ 5 本すべて 0）<br>`10e81cc` **light テーマのコントラスト不足 49→0**（判定色 3 つをトークン化・`--green-bright` `--text-3` を濃く）+ 641〜820px 帯のピルはみ出し（267→225px）<br>`eb8fbba` `/whats-new` の fold 導線（押せる要素 0→4）<br>`b464500` ページ見出しを 34px で統一（`/explore` `/categories` `/favorites` が 22px だった）<br>`121fc9b` `--text-1` を定義（17 箇所が継承値に落ち `.chip-btn:hover` は無効だった）・`.section-link` の二重定義を統合・モバイルの操作要素 26〜31→36px<br>`cd0590b` スパークラインの線幅固定と末端ドット HTML 化（4.2×3.4 の楕円→3.0×3.0）・フォーカス表示 1.4→3.70:1（light）/ 8.15:1（dark）<br>`1b54cac` `/compare` の選択前プレビュー枠（fold の空白 350px→0）<br>`5ac8583` 入力欄・ボタンの枠を `--control-border` に分離（1.4→3.26/3.45:1）<br>`b438ac4` **`.section` / `.hero` の padding 短縮指定が `.container` の左右余白を打ち消し、375px で本文が x=0 に接していた**（全ルート 16px 以上に。1280 でも Home 45px 対 ランキング 73px の食い違いを解消）+ `/categories` の準備中 6 分野を枠付きカードに<br>`8b14e91` タイムラインの丸のリング（`var(--bg-1)` 未定義で box-shadow ごと無効だった） |
| 検証（最終） | はみ出し要素 **11 ルート × 375 / 768 / 1280 / 1920 すべて 0**・11px 未満のテキスト **0**・55 全角超の行は意図的な 2 件のみ・light / dark ともコントラスト不足 **0**（11 ルート）・CSS の重複セレクタ **0**・未定義トークン参照 **0**・`npm run check` 全情報源健全・`npm run build` 成功・score `[89,81,78,72,58,51,37,35,33,32]` 不変・console error 0<br>**本番**: 配信 CSS/JS に全変更を確認。1280 の詳細ページで SVG 内 `<text>` 0 個・目盛りズレ 0・ドット 7×7 と 3×3 の真円・light のコントラスト不足 0 |
| 未完了 | なし（着手して途中のものは無い） |
| 次に行うこと | ①`/timeline` の右側の余白が左より 10px 広い（items 1115px 対 container 1156px）②`.disclaimer` は 64 全角で残置（箱物のため意図的・再検討は不要）③768px の最終スイープは 5 時間制限で中断したので、次回まず実行する |
| 重要な注意点 | **計測の落とし穴 3 つ**: ①`getComputedStyle().backgroundColor` は `color-mix` 由来だと `oklab()` 文字列で返る（RGB として読むと誤判定。oklab→sRGB 変換が必要）②テーマを実行時に切り替えると `transition` が凍結して古い色が残る（`localStorage` に入れて再読み込みしてから測る）③**ResizeObserver も window resize も 1 度も発火しない**（ペイン非表示でフレームが回らない。screenshot も同じ理由で失敗する）→ 幅に追従する実装は検証できないので**読み込み時点で正しい静的 CSS で解く**<br>SVG の `flex-basis` を `auto` にすると viewBox 由来の固有幅（677px）が min-content になり 375px でページが横に膨らむ。`flex: 1 1 0` + `min-width: 0` を維持<br>**同一要素に付く 2 つ目のクラスで `padding` 短縮指定を書くと、`.container` の左右余白が消える**（`padding-block` を使う）<br>読み上げ順序は問題なしと確定<br>未使用 export の検出スクリプトが壊れている（per-name grep 方式に戻す） |

### 作業中に見つけた「別作業」の記録先

`/work` 中に現在の作業と関係ない改善点を見つけた場合、**実装せずここに積む**（W3）。

| 見つけた日 | 内容 | どの作業でやるか |
|---|---|---|
| 2026-07-30 | `/whats-new` は fold 内の押せる要素が 0 個（活動カレンダーのみ） | UI改善 |
| 2026-07-30 | 未使用 export の検出スクリプトが壊れている | コード品質 |

---

## 需要拡張モードの状態（O10）

「需要拡張」の 1 語で実行するモードが読み書きする状態。**ENGINE 第 3.5 章とセットで使う。**

### 実行コマンド

```bash
cd "/c/Users/Owner/Desktop/claude作業用/demand-atlas" && npm run themes:eval
```

### 情報量ベースライン（唯一の評価基準・11 指標）

**毎回この表を実測で更新する。1 つも増えていなければ成果ゼロ、コミットしない。**
テーマだけでなく**情報源・カテゴリ・分析・比較・可視化・データ**も情報量として数える
（2026-07-30 ユーザー指示で軸を 7 → 11 に拡張。下 5 行が今回の初測定）。

| 指標 | 現在 | 前回比 | 測定元 |
|---|---|---|---|
| 公開テーマ数 | 10 | ±0 | `themes:eval` |
| **観測できている情報源数** | **7** | 初測定 | `_*Detail` 6 種（wikipedia / qiita / arxiv / appstore / github / ndl）+ ニュース RSS。派生の `_growthDetail` は除く |
| 使用カテゴリ数 | 3 / 9 | ±0 | `themes:eval` |
| 平均ソース数 | 5.0 | ±0 | `themes:eval` |
| **分析の種類数** | **13** | 初測定 | `_insights` の分析キー（dataQuality / verdict / whyTrending / momentum / beginnerFriendliness / competition / audience / problems / monetization / content / saas / similarThemes / nextActions）。version・generatedAt・method はメタなので除く |
| **比較軸の数** | **5 軸 + 4 セクション** | 初測定 | `Compare.jsx` の `ScoreCell` 10 個 = 5 軸 × 2 列、加えて 情報源バー / 3 ゲージ / アイデア / キーワード分類 |
| **可視化の種類数** | **9** | 初測定 | 折れ線 `chart-svg` / スパークライン `spark-svg` / 分析ゲージ `insight-gauge` / 比較ゲージ `cmp-gauge` / スコア内訳バー `score-bars` / 活動カレンダー `activity-grid` / 比較の情報源バー `cmp-src-bar` / 情報源トレンドのセル `source-trends-cell` / 三指標のドット `tc-dot` |
| アイデア件数 | 80 | ±0 | `themes:eval` |
| 日次系列を持つテーマ | 10 / 10 | ±0 | `themes:eval` |
| **UI が参照するフィールド** | **116 / 142** | **+23（93 → 116）** | `themes:eval` |
| Home 総転送（cold） | 164 KB | +1 KB | ブラウザ実測 |

**スクロール位置（2026-07-31 修正）**: 一覧を下まで見てからテーマを開くと、SPA が
スクロール位置を保持するため **ページの途中（scrollY 1800・見出しが画面の 1628px 上）から
始まっていた**。前進時のみ先頭へ戻し、戻る / 進むはブラウザの復元に任せる方式で修正。
本番実測: ランキング 2200 → 詳細は scrollY 0・見出し 172px、戻ると 2200 に復元、375px でも同様。

**アクセス分析コマンド 第 1 回（2026-07-31）**: 稼働後のレビューで重大 1 件・未検証 1 件を検出し修正。
①**レート制限に当たった訪問者が二度と数えられない**欠陥（クライアントが送信済みフラグを
戻していなかった）→ throttle 時は巻き戻して次の遷移で再挑戦、上限も 20→120/分 に。
②**日別キーの 400 日 TTL が本番で効いているか未検証**だった → テストで EXPIRE を検証し、
診断に `todayKeyTtlDays` を追加。**本番で 400 日を確認**。テスト 44/44。

**アクセス分析は稼働中（2026-07-31 本番実測）**: Upstash Redis を接頭辞 `KV` で接続し、
`storeConfigured: true` / `storeReachable: true` を確認。本番の Home に
**「今日訪れた人 1人 あなたを含む」**が描画され、`visits:check` は 訪問者 1 / 新規 1 /
ページ `/`=1 を返した。同じブラウザで再訪しても 1 人のまま、保存を消す（別ブラウザ相当）と
2 人に増えることを本番で確認。console error 0。
**接頭辞は任意でよい実装**（`<接頭辞>_REST_API_URL` と `<接頭辞>_REST_API_TOKEN` の組を形で検出）。
確認手段は `npm run visits:check` と `/api/visit?diag=1`（値は返さず、変数名と到達可否だけ）。
GET はエッジで 60 秒キャッシュするため、表示値は最大 60 秒古いことがある（初回訪問者は
POST の応答を見るので常に最新）。

**「今日訪れた人」が本番で見えなかった理由（2026-07-31 実測）**: 実装は本番の JS に入っている
（`today-visitors` / `今日訪れた人` / `/api/visit` の文字列を配信バンドルで確認）が、
**KV 未接続のため API が `{"available":false,"reason":"store-not-configured"}` を返し、
UI は何も描画しない**（0 人と嘘をつかないため）。**ローカルで本番ビルド + 本番と同じ
handler + 実 HTTP + KV 互換モックを立てて end-to-end 検証済み**: 「今日訪れた人 1人
あなたを含む」を描画、3 回リロードしても 1 人、保存を消す（別ブラウザ相当）と 2 人、
`/rankings` を見るとページ別に 1 件加算され訪問者数は 2 のまま。
→ 残るのは **Vercel Storage → Redis（無料枠）を Connect して再デプロイ**の 1 手順のみ。

**本番検証（2026-07-31）**: `/api/visit` は GET / POST とも
`{"available":false,"reason":"store-not-configured"}` を JSON で返す（rewrite 修正前は
SPA の HTML を返していた）。SPA のルート（`/` `/rankings` `/demand/:id`）と
`/data/demands.json` は無傷、Home に pill は出ない、console error 0。

**2026-07-31 追加**: 「今日訪れた人」（`/api/visit`）を実装し、**アクセス分析基盤**として
一般化した（`schema: visits/1.1`）。指標は `visits` / `new` / `returning`、次元は
`page` / `referrer`。**指標や切り口の追加は `api/_schema.js` の表に 1 行足すだけ**で、
エンドポイントも保存層も UI も書き換え不要（実測: 指標を 1 行足すと GET の応答に自動で出る）。
これは需要データではなく**利用状況の計測基盤**なので上の 11 指標には数えない。ただし長らく最大のボトルネックだった
「共有しても効果を観測する手段が無い」を解く最初の一手であり、**KV を接続した日から
日次のユニーク訪問者数が測れるようになる**（未接続の間は API が `available:false` を返し
Home には何も出ない）。

**`themes:eval` は 6 指標しか出力しない。** 残る 5 指標（情報源数・分析・比較・可視化・転送）は
現在この表の測定元の記述に従って手で測る。**次回の STEP 1 で `themes:eval` 側へ寄せる**
（同じ数値を 2 箇所で計算しないため）。

**2026-07-30 のスプリントで初めて情報量が増えた。** テーマ数ではなく
**すでに配信していたのに表示していなかったフィールド**を出すことで達成（転送増は JS の +1 KB のみ）。

追加した表示:
- 詳細ページ「情報源ごとの実数」= 5 情報源 × 18 個の実数（NDL 書誌 792 / GitHub 新規 repo 54・主要言語 / arXiv 著者 219・平均著者数 / Wikipedia 7 日窓 865 対 1,812 など）
- 比較ページに **総合判定 と 観測の確かさ**（詳細にはあったが比較には無かった）
- ランキングの指標を volume 固定から **反応（LGTM / star）/ 関与人数（投稿者 / 著者）** へ拡張（+39 行。GitHub star が payment-troubles で +68.1% など、volume では見えない動きが出る）
- カードに **ニュース件数 と 情報源数**（根拠の量）
- Home Hero に **事業アイデア 80**

**フィールド数の定義**: `demands[0]` の全キー（ネスト含む・日付キー除外）を `src/**/*.{js,jsx}`
の全文と照合。以前 STATE に載っていた「68/110」はリーフのみを数えた別定義のため接続しない。

### ループの終了条件（STEP 1・STEP 4）

- 1 周で検出した改善点が **0 件**
- **2 周連続**で新規の改善点が 0 件
- **3 周に到達**（残りは STATE に繰り越す）

### 現在のレジストリ（`config/theme-registry.json`）

| 状態 | 件数 | 内訳 |
|---|---|---|
| active | 10 | 公開中 |
| **stalled** | 1 | `ai-content-generation`（マッピングも辞書も揃っているがキーワードがヒットしない） |
| **candidate** | 1 | `entertainment-content`（Phase A 未実施） |
| observing | 0 | マッピングのみ投入した段階のテーマはまだ無い |
| retired | 0 | まだ無い |

昇格基準 `minSources: 3` / `minNewsArticles: 5`、降格基準 `consecutiveDaysBelow: 3`。
**status はレジストリの自己申告ではなくファイル実体から導出して突き合わせる**
（マッピング数 / wikipedia マップ / キーワード辞書の有無）。現在の食い違いは 0 件。

### 昇格は 2 段階（ENGINE 第 3.5 章 STEP 7）

| Phase | 編集するもの | 効果 |
|---|---|---|
| **A 観測開始** | `config/*-mapping.json` × 5 + `fetch-wikipedia-pageviews.mjs` | 取得は始まるが**公開されない**。ここで初めてソース数を実測できる |
| **B 公開** | `extract-demand-candidates.mjs` の辞書 + `themeCatalog.js` | 公開。実測でソース数を満たしたテーマだけ |

**「昇格基準を実測で満たす」を 1 段階目の条件にはできない**（マッピングに入れるまで測れない）。
既存アーキテクチャが取得＝マッピング駆動 / 公開＝辞書駆動で分かれているため、この分割が成立する。

### 評価 8 軸の測定手段（STEP 3）

| 軸 | 測り方 | 測れる時期 |
|---|---|---|
| 継続性 | 分散 = 出現日数 ÷ 件数（事件 0.1〜0.29 / 継続語 1.0） | 今すぐ |
| 情報量 | 未紐付け記事での出現件数 | 今すぐ |
| カテゴリ適合性 | 空白カテゴリを埋めるか | 今すぐ |
| 重複 | 既存 keywords / 却下語との一致数 | 今すぐ |
| 取得コスト | 編集ファイル数（Phase A 6 / Phase B 2）と API 呼び出し数 | 今すぐ |
| 将来性 | `keyword-trends` の velocity。**新語は判断材料不足** | 語が trends に入った後 |
| 取得可能性 | 各情報源の検索語が 0 件でないか | Phase A |
| 需要性 | Qiita 記事数 + App Store アプリ数 | Phase A |

**需要性をニュース記事の意図語共起で測る案は却下**（2026-07-30 実測）。
ゲーム 7% / アニメ 0% / 音楽 0% に対し、無関係な新聞 16% / SNS 29% が上位に来て判別しない。

### 候補の優先順位式（STEP 6 の再現性）

```
分散     = 出現日数 / 出現件数     （事件 0.1〜0.29 / 継続語 1.0 — 2026-07-30 実測）
優先度   = 出現件数 × 分散
採用条件 = 出現 5 件以上 かつ 分散 0.6 以上 かつ 却下語・既知語でない
```

### 現在の候補

**なし。** 却下語 95 語を除外した後、残る上位は `Google`（企業名）・`対策`（汎用語）で、
テーマ候補として成立するものが無い。**corpus（RSS 4 本・691 件）から出る候補は枯れた。**

### 情報源の自律調査（ENGINE 第 3.5 章 STEP 2.5・2026-07-30 追加）

**発動条件は実測で 2 つ。** ①候補が 0 件 ②空白カテゴリが現在の情報源では埋まらないと結論済み。
**現在は両方を満たしている**ため、次回の「需要拡張」は情報源調査から始めるのが正しい入口。

| 手順 | 判定 |
|---|---|
| 既知の調査結果を先に読む | 下の「情報源の調査結果（再調査不要）」12 件は再調査しない |
| 絶対条件で機械的に落とす | 有料 / キー必須 / スクレイピング前提 / ToS 不許可 → 即却下 |
| 生存確認 | 1 回叩いて 200 と**中身**を確認（200 だけでは判断しない・R6） |
| 日本語・日本市場の信号 | 英語圏専用は不採用（Hacker News の前例） |
| 既存との重複 | 同じテーマで同じ順位を返すだけなら価値ゼロ（OpenAlex × arXiv の前例） |
| RSS の場合 | **テーマ紐付き率 0% は不採用**（下の採用基準表） |
| 記録 | 採否と理由を調査結果表へ。**却下も残す** |

採用後は既存情報源と同じ扱い（envelope v1.0.0 / `metricKind` 明示 / `check-sources` 登録 /
history 記録 / UI 表示）まで到達して初めて完了。

**次に調べる優先順**: ①技術・製品メディアの RSS（紐付き率 24% 系・probe 済み 7 本）
②政府・自治体のオープンデータ（e-Stat は API キー必須で却下済み → キー不要の代替を探す）
③非 tech テーマ限定の学術ソース（OpenAlex を senior-health だけに絞る案は保留中）

### 却下した候補テーマ

| id | 却下理由（実測 2026-07-30） |
|---|---|
| `entertainment-content`（ゲーム・アニメなどエンタメ制作） | 全 36 件を読んだ結果、**制作・開発の需要は 8〜9 件（25%）のみ**。音楽 6 件（性加害報道 / ギター / フジロック / シニアバンド / 竹島 / 始球式）とマンガ 6 件（作品更新通知・推薦図書）は**需要信号 0 件**で、根拠 30 件のうち 12 件が無価値。アニメ制作系は 4〜5 件で昇格基準（news 5 件）未達。ゲーム開発系 2 件は Zenn 由来で `ai-coding` と重複。**前サイクルの「計 30 件」は語の出現件数を需要の強さと混同した過大評価** |

### 却下した評価指標（再試行しない）

| 指標 | 実測による否定 |
|---|---|
| 需要性 = 意図語の共起（したい / 方法 / 対策 …） | ゲーム 7% / アニメ 0% / 音楽 0% に対し、無関係な 新聞 16% / 日本経済 25% / SNS 29% が上位。判別しない |
| 需要性 = 技術系ソース（Zenn + Qiita）比率 | 既存 10 テーマの比率は 0.00〜1.00 で、**うち 6 テーマが 0.00**。0.00 は失格条件にならない |

→ **需要性はニュース corpus から測れない。** Phase A の Qiita 記事数 + App Store アプリ数で測る現行仕様が正しい。

### 評価ログ（`data/theme-evaluations.jsonl`）

| 項目 | 値 |
|---|---|
| 蓄積日数 | **1 日**（2026-07-30） |
| 降格判定に必要な日数 | 3 日（`themes:eval` を 3 回以上、別の日に動かす必要がある） |
| 記録している内容 | date / published / registered / belowCriteria / active[] / observing[] / seedTop[] |

### 却下した候補語

**散文ではなく `config/theme-registry.json` の `seedFilter.rejectedWords` に置いた**（72 語）。
散文だと STEP 4 の重複除去が実際には機能せず、毎回同じ固有名詞が再登場していた。
現在 32 語が自動で除外されている。

| 分類 | 却下理由（実測） |
|---|---|
| 固有名詞・事件名（20 語） | 未紐付け記事 607 件の頻出語は事件が支配する。分散 0.1〜0.29 |
| 汎用語（25 語） | velocity は高い（12〜26.5 件/日）が特異度が無く、既存テーマと分離できない |
| 既存テーマと分離できない語（27 語） | `生成AI` は 16 件ヒットのうち内容一致 3 件程度。corpus 自体に信号がない |

### フィード採用基準（第 2 サイクルで確定・実測）

**採否は「テーマ紐付き率」= そのフィードの記事のうち需要テーマの根拠になった割合で決める。**

| フィード | 記事 | 紐付き | 率 |
|---|---|---|---|
| ITmedia | 182 | 44 | **24%** |
| Zenn | 82 | 20 | **24%** |
| はてブ総合 | 382 | 18 | 5% |
| NHK | 74 | 2 | 3% |
| **はてブ 経済（試行→撤回）** | 21 | **0** | **0%** |
| **はてブ 暮らし（試行→撤回）** | 18 | **0** | **0%** |
| **はてブ 学び（試行→撤回）** | 24 | **0** | **0%** |

**紐付き率 0% のフィードは採用しない。** 一般ニュースの分野別フィードには需要
（作りたい / 買いたい / 困っている）が現れず、候補語の上位が トランプ・ロシア・自分・現場 と
国際政治や汎用語に置き換わって発見の質が下がった。

紐付き率 24% の 2 本はいずれも**技術・製品メディア**。次に試すのは同系統に絞る
（probe 済み・生存確認済み: ITmedia AI+ 20 / ITmedia ビジネス 20 / Qiita 人気 30 /
CodeZine 20 / 窓の杜 60 / GIGAZINE 30 / はてブ IT 30）。

### 空白カテゴリについての実測結論

**起業・副業・教育・生活・エンタメ・美容の 6 分野は、現在の情報源設計では埋まらない。**
分野別の一般ニュースを足しても需要は現れない（上記 0%）。埋めるには分野ごとに
「需要が現れる媒体」を選ぶ必要があり、求人系・EC 系は絶対条件（ToS / キー不要）で除外済み。

### 次回の実行で変わる条件

| 条件 | いつ |
|---|---|
| **新しい候補が出る** | **技術・製品メディアを 1〜2 本足した時**（紐付き率 24% 系）。一般ニュースの追加は却下済み |
| 降格判定が有効になる | `themes:eval` を別日に 3 回動かした後（現在 1 日分） |
| `ai-content-generation` が公開され得る | ルールベース照合の根拠記事が 1 件以上になった時（現在 0 件） |

---

## 決定済みの方針

- **絶対条件**: 完全無料 / API キー原則不要 / スクレイピング禁止 / ToS 遵守 / `.env` の中身を出さない
- **score 計算式は変更しない**: `40×ニュース量 + 30×直近成長 + 20×ニュース媒体の多様性 + 10×鮮度`
  表示層だけの変更でも、score / status が**不変であること**を毎回確認する
- **昇格は 2 段階**（Phase A = マッピングのみ / Phase B = 辞書追加で公開）
- git identity は local `RoLororo`（global は別プロジェクト用。触らない）
- **テーマ追加は「3 ソース以上 かつ news 5 件以上」を満たすものに限る**
  （実測: 2 ソース以下のテーマは停止率 60〜70%）

---

## 測定状況

### 測定済み（再測定不要）

CI / データ品質 / カバレッジ / 静止率 / カテゴリ多様性 / evidence 鮮度（14 日超 0 件）/
history スキーマ / 本番データ同期 / 本番実描画 / 転送量 / bundle / SEO / npm audit /
envelope complete・coverage / archive の不在 / コスト（Actions 分数・帯域損益分岐）/
アクセシビリティ（h1・ラベル・コントラスト・フォーカスリング）/ schedule の実配信時刻 /
コード品質（lint・test・未使用 export・tree-shaking）/ OG カードの内容 / 判定の遷移率

### 未測定

ドキュメント / 開発フロー / リリース方法 / 実コスト / API 準備 / 読み上げ順序

### 実測不能（代替案を毎回検討する）

| 項目 | 理由 | 代替案 |
|---|---|---|
| 実ユーザー数 | ~~アナリティクス未導入~~ → **計測手段を実装済み**（`/api/visit`・日次ユニーク訪問者）。**KV 未接続の間は数えられない** | Vercel Storage → Redis（無料枠）を Connect し `KV_REST_API_URL` / `KV_REST_API_TOKEN` を入れる。接続後は Home に表示され、GET で 昨日 / 今週 / 今月 / 累計 も取れる |
| OGP のクローラー挙動 | クローラーを再現できない | **画像の内容は目視で検証済**（2026-07-30）。残る不確実性は各 SNS のトリミングのみ |
| Vercel dashboard の数値 | 権限なし | **代替算出で解決済**（131 KB × 訪問数 vs 100 GB） |
| Vercel Hobby の商用利用可否 | 規約を実測できない | 収益化の着手前に規約本文を確認する |
| GitHub Actions の通知設定 | 認証が必要で確認できない | **ユーザーに確認を依頼**（cancelled が failure 通知の対象か含む） |

---

## 実装の事後検証（O7）

検証が済んだ項目は削除する（更新規約）。

| 実装 | 期待した効果 | 実際の効果 | 予測との差 |
|---|---|---|---|
| OG カード画像 | 共有時に画像が表示される | 本番で `image/jpeg` 44,331B・1200x630・ローカルとバイト一致 | 予測通り。**実際のカード表示は共有発生まで検証不能** |
| 需要拡張 第 2 サイクル（コーパス拡張） | RSS を 4→7 本に増やして新しい候補を得る | **紐付き 63 件中 0 件（0%）**。候補語の上位が トランプ・ロシア・自分・現場 に置き換わり**発見の質が低下**したため撤回。既存テーマへの影響も 0（score 差は既存フィードの通常更新に由来） | **予測が外れた**。「フィードを増やせば候補が増える」と考えたが、媒体の性質が決定的だった。紐付き率という選定基準を得たのが唯一の成果 |
| 需要拡張 第 1 サイクル | 候補を 1 件昇格させ情報量を増やす | **情報量の増加ゼロ**（テーマ・カテゴリ・アイデア・参照フィールドすべて ±0）。成果は精度改善と誤候補の除去のみ。却下語 72→95 語、却下指標 2 件を記録 | **前サイクルの候補が誤りだった**。記事例を 2 件しか見ずに「30 件の根拠」と評価しており、全 36 件を読むと需要は 25% だった。**出現件数を需要の強さと混同する癖**を検出 |
| 需要拡張コマンドの再設計（STEP 1〜10） | 目的をテーマ追加から「情報量の増加」へ広げ、ループの終了条件と評価軸の測定手段を確定させる | ENGINE 第 3.5 章を全面改訂（STEP 10 個 / 絶対ルール X1〜X8）。情報量ベースラインを `themes:eval` が自動出力（公開 10 / カテゴリ 3-9 / アイデア 80 / 参照フィールド 93-142）。**README の陳腐化 6 箇所を同時に修正**（「すべてモックデータで動作」「12 件」「情報源接続は今後の課題」など） | **想定外の発見**: 整合性確認（STEP 7）を自分のコマンドに適用した結果、README が実態から大きく乖離していた。11 回のレビューで一度も README を見ていなかった |
| テーマレジストリ + `evaluate-themes.mjs` | テーマの状態と昇格・降格判定を機械が答えられるようにする | レジストリ 11 件と実データの不整合 **0**、基準未達 **3/10** を自動検出。降格の 3 日ルールは **隔離環境（`DATA_ROOT` 差し替え）で発火／非発火の両方を検証**。2 回実行してログは 1 日分のまま（冪等） | **予測通り。既存への影響ゼロ**（`PATHS.source` 9 キー・`config` 5→6 キーの追加のみ、`npm run check` は全情報源 OK）。想定外は storage adapter の API を `read` と誤って書いた 1 件（実行して即判明・修正） |
| 推移グラフの系列差し替え | 破綻表示（90d 非ゼロ 6% / y 軸上限 5 / 全ゼロ 2 テーマ）の解消 | **全 10 テーマで 29 点描画・平坦ゼロ 0 件**（uniqueY 12〜28）。y 軸 0→120 台、x 軸は実日付、90 日タブ廃止、凡例で系列名を明示、ニュース件数は注記へ退避。console error 0 / 375・1280 ではみ出し 0。**本番（`index-COA_iwpO.js`）でも 29 点・凡例・実日付軸を確認** | **予測通り。副作用ゼロ**。転送は 162→163 KB（+1 KB = JS のみ。`byDate` は既に配信済みだったためデータ側の増分ゼロ） |
| 表示事実の是正（更新時刻・情報源） | 実態と表示を一致させる | 本番の 4 ページ・データ 10 テーマで `06:00` 出現 **0**。description も実情報源に。**CI が再生成した後も維持**（`3052db6`）。スコア差分 0 | **予測通り。副作用ゼロ**。文言が伸びた 2 箇所も 375/1280 ではみ出し 0 |
| workflow timeout 10→30 | 欠測率 25% の解消 | **効果確定**。修正後初回（07-30）は **633s** で、旧 10 分なら確実に cancelled。3 日連続成功 | 予測通り。ただし**実行時間が伸びる方向**（max 548→633s）は予測しておらず、1200s しきい値までの余裕は 53% |
| GitHub 追加 | 静止率の改善 | **静止率 30%（3/10）で全ソース中最小**＝最も動く。ただし全体は 37→40%（arxiv 44→47% / wikipedia 31→38% の悪化が上回った） | 方向は予測通りだが**全体値は改善しなかった**。1 ソース追加で全体平均は動かせない |
| NDL 追加（stock 除外） | 静止指標を静止率計算から外す | **除外は正しく機能**。NDL 単体は **100%（11/11）静止**で、含めていれば全体 40→43% に悪化していた | 予測通り。`metricKind: 'stock'` の設計判断が実測で裏付けられた |

**判明した予測の癖**: 直接の効果は 7/7 で的中。**波及の予測は 3 回連続で外した後、対策 1・2 の導入以降は 2 回連続で副作用ゼロ**（ThemeLink のレイアウト崩れ 1 件を除く）。
- 対策 1（有効）: 実装前に「前提としている記述・表示・設定は他にどこにあるか」を grep で洗う
- 対策 2（有効）: 一覧・順序・件数を持つ定数（`*_ORDER` / `*_COUNT` / リテラル数値）も grep 対象に含める
- 対策 3（新規・2026-07-30）: **要素の種類を変える時は、その要素に依存した CSS セレクタ（`a`・`button` 限定など）を先に grep する**。`ThemeLink` で `a` → `span` にした際、`.db-movers-list a` 限定のレイアウトが外れて行が崩れた
- 未解決の癖: **非機能指標の副作用**。GitHub/NDL 追加時に転送量 +12KB を、timeout 延長時に実行時間の伸長（548→633s）を、いずれも事前に見積もっていなかった

## 次回レビュー計画（O6）

| 項目 | 内容 |
|---|---|
| **最優先テーマ** | `実装GO` が出た場合はレジストリ／評価スクリプトの事後検証（O7）。出ていない場合は**判定遷移率の確定**（07-30 → 07-31 の日次コミットで 24h の status / verdict / 順位を測り、9.5h=1/10・旧式 24h=2/10 と突き合わせる） |
| **次に調査すべきこと** | ① 上記のいずれか<br>② 4 日連続成功か＋実行時間（633s の次の値）<br>③ dataQuality 100 到達テーマの内訳（持ち越し）<br>④ **追跡数を増やす場合の律速**: 9 ファイル手編集と、RSS 4 本という発見上限のどちらが先に効くか |
| **起動方法** | **「レビュー」の 1 語**。実行内容は本セクションが決める（プロンプトは再生成しない） |

## 自己評価履歴

直近 3 回のみ保持（更新規約）。

| 回 | 点数 | 次回改善する点 |
|---|---|---|
| 31 | 92 | **評価軸を実装前に検証して 1 つ捨てた**。「需要性 = ニュース記事の意図語共起」を測ったら ゲーム 7% / アニメ 0% に対し無関係な 新聞 16% / SNS 29% が上位に来たため却下し、Phase A の Qiita / App Store 実測へ置き換えた。減点は **README を 11 回のレビューで一度も見ていなかった**こと（「すべてモックデータで動作」と書かれたままだった）。整合性確認の対象に README を明示的に入れる |
| 32 | 91 | **自分が前サイクルに登録した候補を、実測で自分で却下した**（36 件全読み → 需要は 25%、音楽・マンガは 0 件）。代理指標 2 つも実測で否定し、無理に採用しなかった。減点は、前サイクルで**記事例を 2 件しか見ずに 30 件の根拠と評価した**こと。件数と需要の強さを混同する癖があり、対策として上位語は 6 例＋残件数を必ず表示するようにした |
| 33 | 90 | **自分の変更を実測して撤回できた**（紐付き 0/63）。フィードを増やす前にスコアを記録し、副作用を切り分けたうえで、コードとデータの両方を完全に巻き戻した。減点 2 点: ①「フィードを増やせば候補が増える」を検証前に実装してしまった（probe で生存だけ見て、媒体の性質を見ていなかった）②置換の assert 失敗に気づかず先へ進みかけた |
